# Быстрая инструкция: Как найти и установить Android SDK

## 🎯 Самый простой способ

### Способ 1: Через меню Tools (рекомендуется)
1. Откройте Android Studio
2. В верхнем меню нажмите **Tools → SDK Manager**
3. Откроется окно **SDK Manager**

### Способ 2: Через поиск в настройках
1. Откройте Android Studio
2. Нажмите **File → Settings** (или **Ctrl + Alt + S**)
3. В поле поиска вверху введите: **SDK**
4. Выберите **Languages & Frameworks → Android SDK**

### Способ 3: Через иконку в панели инструментов
- Найдите иконку **SDK Manager** (обычно в верхней панели рядом с другими инструментами)
- Кликните на неё

---

## 📦 Установка Android SDK

### Если SDK Manager открылся:

1. **Перейдите на вкладку "SDK Platforms"**
2. **Установите флажки:**
   - ✅ **Android 14.0 (API 34)** - для целевой версии
   - ✅ **Android 8.0 (API 26)** - минимальная версия для проекта

3. **Перейдите на вкладку "SDK Tools"**
4. **Установите флажки:**
   - ✅ **Android SDK Build-Tools** (обязательно)
   - ✅ **Android SDK Platform-Tools** (обязательно)
   - ✅ **Android SDK Command-line Tools** (обязательно)
   - ✅ **Android Emulator** (если будете использовать эмулятор)
   - ✅ **Google Play services** (опционально)

5. **Нажмите "Apply" → "OK"**
6. **Дождитесь загрузки** (может занять несколько минут)

---

## 🔧 Если SDK Manager не открывается

### Проверка пути к SDK:

1. **File → Settings → Languages & Frameworks → Android SDK**
2. Проверьте поле **Android SDK Location**
3. Обычные пути:
   - **Windows:** `C:\Users\ВашеИмя\AppData\Local\Android\Sdk`
   - **Mac:** `~/Library/Android/sdk`
   - **Linux:** `~/Android/Sdk`

### Если SDK не найден:

1. В том же окне нажмите **Edit** рядом с полем **Android SDK Location**
2. Выберите существующую папку или создайте новую (например, `C:\Android\Sdk`)
3. Нажмите **Next** → **Finish**
4. Android Studio автоматически загрузит необходимые компоненты

---

## 🌍 Настройка переменных среды (Windows)

### Если нужно настроить ANDROID_HOME:

1. Откройте **Панель управления → Система → Дополнительные параметры системы**
2. Нажмите **Переменные среды**
3. В разделе **Системные переменные** нажмите **Создать**
4. Введите:
   - **Имя переменной:** `ANDROID_HOME`
   - **Значение переменной:** путь к SDK (например, `C:\Users\ВашеИмя\AppData\Local\Android\Sdk`)
5. Нажмите **OK**
6. **Перезапустите Android Studio**

---

## ✅ Проверка установки

После установки SDK проверьте:

1. **SDK Manager → SDK Platforms**
   - Должны быть установлены API 26 и API 34

2. **SDK Manager → SDK Tools**
   - Должны быть установлены Build-Tools, Platform-Tools, Command-line Tools

3. **File → Settings → Languages & Frameworks → Android SDK**
   - В поле **Android SDK Location** должен быть указан правильный путь

---

## 🆘 Если ничего не помогает

1. **Переустановите Android Studio:**
   - Полностью удалите Android Studio
   - Скачайте последнюю версию с [официального сайта](https://developer.android.com/studio)
   - Установите заново, убедившись, что выбрана опция установки Android SDK

2. **Проверьте системные требования:**
   - Убедитесь, что у вас достаточно места на диске (минимум 10 GB)
   - Проверьте подключение к интернету

3. **Обратитесь за помощью:**
   - [Официальная документация Android](https://developer.android.com/studio/intro)
   - [Stack Overflow](https://stackoverflow.com/questions/tagged/android-studio)

---

## 📝 Полезные команды для проверки

### Проверка через командную строку (Windows):

```powershell
# Проверка переменной ANDROID_HOME
echo $env:ANDROID_HOME

# Проверка наличия SDK
Test-Path "$env:ANDROID_HOME\platform-tools\adb.exe"
```

### Проверка через командную строку (Mac/Linux):

```bash
# Проверка переменной ANDROID_HOME
echo $ANDROID_HOME

# Проверка наличия SDK
ls $ANDROID_HOME/platform-tools/adb
```

---

**После установки SDK вернитесь к файлу `RUNNING_INSTRUCTIONS.md` для продолжения настройки проекта.**
