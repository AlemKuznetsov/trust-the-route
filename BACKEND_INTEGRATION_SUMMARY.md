# ✅ Интеграция бэкенда завершена

## Что было сделано:

### 1. ✅ Обновлен BASE_URL
- **Файл:** `app/src/main/java/com/trusttheroute/app/di/NetworkModule.kt`
- **URL:** `http://158.160.180.232:8080/api/v1/`
- **Примечание:** Для продакшена замените на домен с HTTPS

### 2. ✅ Обновлен AuthApi
- Добавлен `MessageResponse` для ответов с сообщениями
- Endpoints соответствуют бэкенду

### 3. ✅ Обновлен AuthRepository
- Добавлена обработка ошибок из API
- Улучшена обработка сетевых ошибок
- `resetPassword` возвращает сообщение от сервера

### 4. ✅ Обновлен AuthViewModel
- Добавлены функции `register()` и `resetPassword()`
- Добавлено состояние `AuthUiState.Message` для сообщений

### 5. ✅ Реализованы экраны
- **RegisterScreen** - полная реализация с валидацией
- **ResetPasswordScreen** - полная реализация

### 6. ✅ Настроено логирование
- В DEBUG режиме логируются все запросы
- В продакшене логирование отключено

## 📱 Как использовать:

### Регистрация пользователя:
1. Откройте приложение
2. Нажмите "Регистрация"
3. Заполните форму (имя, email, пароль)
4. Нажмите "Зарегистрироваться"
5. После успешной регистрации вы автоматически войдете в приложение

### Вход:
1. Откройте приложение
2. Введите email и пароль
3. Нажмите "Войти"
4. Или используйте "Войти через Yandex ID"

### Восстановление пароля:
1. На экране входа нажмите "Забыли пароль?"
2. Введите email
3. Нажмите "Отправить"
4. Вы получите сообщение (в реальном приложении будет отправлено письмо)

## 🔧 Настройка для продакшена:

1. **Замените BASE_URL** на домен с HTTPS:
   ```kotlin
   private const val BASE_URL = "https://api.trusttheroute.com/api/v1/"
   ```

2. **Настройте домен** для вашего сервера

3. **Проверьте SSL сертификат** (если используете самоподписанный, нужно настроить TrustManager)

## 🧪 Тестирование:

1. Запустите приложение
2. Попробуйте зарегистрироваться
3. Попробуйте войти
4. Попробуйте восстановить пароль
5. Проверьте логи в Logcat для отладки

## 📝 Примечания:

- Все ошибки API правильно обрабатываются и показываются пользователю
- Токены автоматически сохраняются при успешной авторизации
- Состояние авторизации проверяется при запуске приложения
