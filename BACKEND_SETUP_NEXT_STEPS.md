# Следующие шаги настройки бэкенда

Руководство по дальнейшей настройке сервера для бэкенда Trust The Route.

---

## ✅ Что уже сделано

- ✅ PostgreSQL установлен
- ✅ База данных `trust_the_route` создана
- ✅ Пользователь `trust_user` создан и настроен
- ✅ Права доступа настроены

---

## 🔍 Проверка подключения к базе данных

Проверьте, что подключение к базе данных работает:

```bash
# Подключиться к базе данных от имени нового пользователя
psql -U trust_user -d trust_the_route -h localhost
```

**Если подключение успешно**, вы увидите:
```
trust_the_route=>
```

**Проверьте подключение:**
```sql
-- Проверить текущую базу данных
SELECT current_database();

-- Проверить текущего пользователя
SELECT current_user;

-- Выйти
\q
```

---

## 📦 Установка Java (JDK 17)

Для Kotlin + Ktor бэкенда нужна Java:

```bash
# 1. Обновить список пакетов
sudo apt update

# 2. Установить Java JDK 17
sudo apt install openjdk-17-jdk -y

# 3. Проверить установку
java -version
javac -version
```

**Ожидаемый результат:**
```
openjdk version "17.0.x"
OpenJDK Runtime Environment (build 17.0.x+xx-Ubuntu-xx)
OpenJDK 64-Bit Server VM (build 17.0.x+xx-Ubuntu-xx, mixed mode, sharing)
```

---

## 🔧 Настройка переменных окружения (опционально)

Если нужно установить JAVA_HOME:

```bash
# Найти путь к Java
sudo update-alternatives --config java

# Добавить в ~/.bashrc
echo 'export JAVA_HOME=/usr/lib/jvm/java-17-openjdk-amd64' >> ~/.bashrc
echo 'export PATH=$PATH:$JAVA_HOME/bin' >> ~/.bashrc

# Применить изменения
source ~/.bashrc

# Проверить
echo $JAVA_HOME
```

---

## 🛡️ Настройка Firewall

Убедитесь, что нужные порты открыты:

```bash
# Проверить статус firewall
sudo ufw status

# Открыть необходимые порты
sudo ufw allow 22    # SSH
sudo ufw allow 80    # HTTP
sudo ufw allow 443   # HTTPS
sudo ufw allow 8080  # Backend API (временно, потом через nginx)

# Включить firewall (если еще не включен)
sudo ufw enable

# Проверить статус
sudo ufw status
```

---

## 📋 Итоговый чеклист

Проверьте, что все установлено:

```bash
# 1. PostgreSQL
psql --version
sudo systemctl status postgresql

# 2. Java
java -version
javac -version

# 3. Firewall
sudo ufw status

# 4. Подключение к базе данных
psql -U trust_user -d trust_the_route -h localhost -c "SELECT version();"
```

---

## 🚀 Следующие шаги

После установки Java:

1. ✅ **Создать проект Ktor бэкенда**
   - Настроить структуру проекта
   - Добавить зависимости (Ktor, Exposed, PostgreSQL driver)

2. ✅ **Настроить подключение к базе данных**
   - Создать файл конфигурации
   - Настроить connection pool

3. ✅ **Реализовать API endpoints**
   - `/api/v1/auth/register`
   - `/api/v1/auth/login`
   - `/api/v1/auth/reset-password`

4. ✅ **Настроить JWT аутентификацию**
   - Генерация токенов
   - Валидация токенов

5. ✅ **Настроить nginx** (для production)
   - Проксирование запросов
   - SSL сертификат (Let's Encrypt)

6. ✅ **Обновить BASE_URL в Android приложении**
   - Изменить `Constants.BASE_URL` на адрес вашего сервера

---

## 📝 Параметры подключения к базе данных

Сохраните эти параметры для использования в бэкенде:

```
Host: localhost (или 127.0.0.1)
Port: 5432 (стандартный порт PostgreSQL)
Database: trust_the_route
User: trust_user
Password: ваш_пароль_который_вы_указали
```

**Пример строки подключения:**
```
jdbc:postgresql://localhost:5432/trust_the_route?user=trust_user&password=ваш_пароль
```

---

## ✅ Готово к разработке бэкенда!

Теперь у вас есть:
- ✅ Сервер с Ubuntu
- ✅ PostgreSQL база данных
- ✅ Java для Kotlin/Ktor
- ✅ Настроенный firewall

**Можно начинать разработку бэкенда!**

---

**Выполните проверку подключения к базе данных и установку Java, затем сообщите, если нужна помощь с созданием проекта Ktor!**
