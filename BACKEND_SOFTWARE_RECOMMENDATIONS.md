# Рекомендации по ПО для бэкенда

Руководство по выбору программного обеспечения для бэкенда Trust The Route.

---

## 🎯 Что нужно для бэкенда

Судя по вашему проекту, вам нужен бэкенд для:
- ✅ Регистрации пользователей (`/auth/register`)
- ✅ Входа в систему (`/auth/login`)
- ✅ Сброса пароля (`/auth/reset-password`)
- ✅ Управления маршрутами и достопримечательностями
- ✅ API для мобильного приложения (REST API)

---

## 🏆 Рекомендуемые варианты

### 🥇 Вариант 1: Kotlin + Ktor (РЕКОМЕНДУЮ)

**Почему это лучший выбор:**
- ✅ **Один язык** — вы уже знаете Kotlin (Android приложение)
- ✅ **Единый стек** — один язык для фронтенда и бэкенда
- ✅ **Легковесный** — быстрый старт, низкое потребление ресурсов
- ✅ **Корутины** — отличная поддержка асинхронности (как в Android)
- ✅ **Простая настройка** — минимум конфигурации

**Что нужно установить на сервере:**

```bash
# 1. Java (JDK 17 или выше)
sudo apt update
sudo apt install openjdk-17-jdk -y

# 2. Проверка установки
java -version
```

**Технологический стек:**
- Kotlin + Ktor (веб-фреймворк)
- Exposed (ORM для работы с БД)
- PostgreSQL (база данных)
- JWT (токены аутентификации)

**Плюсы:**
- ✅ Не нужно изучать новый язык
- ✅ Можно переиспользовать код между Android и бэкендом
- ✅ Отличная производительность
- ✅ Хорошая документация

**Минусы:**
- ⚠️ Меньше готовых решений, чем у Node.js
- ⚠️ Меньше библиотек, чем у Python

**Документация:** https://ktor.io/

---

### 🥈 Вариант 2: Node.js + Express (если нужна скорость разработки)

**Почему подходит:**
- ✅ Огромная экосистема библиотек (npm)
- ✅ Быстрая разработка
- ✅ Много готовых решений для аутентификации
- ✅ Хорошая производительность

**Что нужно установить на сервере:**

```bash
# 1. Node.js (версия 18 LTS или выше)
curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash -
sudo apt install -y nodejs

# 2. Проверка установки
node -v
npm -v
```

**Технологический стек:**
- Node.js + Express (веб-фреймворк)
- TypeScript (опционально, но рекомендуется)
- Prisma или Sequelize (ORM)
- PostgreSQL (база данных)
- JWT (токены аутентификации)

**Плюсы:**
- ✅ Огромная экосистема (npm)
- ✅ Быстрая разработка
- ✅ Много готовых решений
- ✅ Хорошая документация

**Минусы:**
- ⚠️ Другой язык (JavaScript/TypeScript)
- ⚠️ Может быть сложнее для Kotlin разработчика

**Документация:** https://expressjs.com/

---

### 🥉 Вариант 3: Python + FastAPI (если нужна автоматическая документация)

**Почему подходит:**
- ✅ Очень быстрая разработка
- ✅ Автоматическая документация API (Swagger)
- ✅ Простой синтаксис
- ✅ Хорошая поддержка асинхронности

**Что нужно установить на сервере:**

```bash
# 1. Python (версия 3.10 или выше)
sudo apt update
sudo apt install python3 python3-pip python3-venv -y

# 2. Проверка установки
python3 --version
pip3 --version
```

**Технологический стек:**
- Python + FastAPI (веб-фреймворк)
- SQLAlchemy (ORM)
- PostgreSQL (база данных)
- JWT (токены аутентификации)

**Плюсы:**
- ✅ Очень быстрая разработка
- ✅ Автоматическая документация API
- ✅ Простой синтаксис
- ✅ Хорошая поддержка асинхронности

**Минусы:**
- ⚠️ Другой язык (Python)
- ⚠️ Может быть медленнее, чем Kotlin/Node.js

**Документация:** https://fastapi.tiangolo.com/

---

## 💡 Моя рекомендация

### Для вашего проекта рекомендую: **Kotlin + Ktor**

**Причины:**
1. ✅ Вы уже знаете Kotlin (Android приложение)
2. ✅ Единый язык для фронтенда и бэкенда
3. ✅ Можно переиспользовать модели данных
4. ✅ Легковесный и быстрый
5. ✅ Отличная поддержка корутин

---

## 📋 Что нужно установить на сервере (для Kotlin + Ktor)

### Шаг 1: Обновить систему

```bash
sudo apt update
sudo apt upgrade -y
```

### Шаг 2: Установить Java (JDK 17 или выше)

```bash
sudo apt install openjdk-17-jdk -y
java -version
```

### Шаг 3: Установить PostgreSQL (база данных)

```bash
sudo apt install postgresql postgresql-contrib -y
sudo systemctl start postgresql
sudo systemctl enable postgresql
```

### Шаг 4: Настроить PostgreSQL

```bash
# Войти в PostgreSQL
sudo -u postgres psql

# Создать базу данных
CREATE DATABASE trust_the_route;

# Создать пользователя
CREATE USER trust_user WITH PASSWORD 'ваш_надежный_пароль';

# Дать права пользователю
GRANT ALL PRIVILEGES ON DATABASE trust_the_route TO trust_user;

# Выйти
\q
```

### Шаг 5: Настроить firewall

```bash
sudo ufw allow 22    # SSH
sudo ufw allow 80    # HTTP
sudo ufw allow 443   # HTTPS
sudo ufw allow 8080  # Backend API (временно, потом через nginx)
sudo ufw enable
```

---

## 🚀 Следующие шаги

После установки ПО:

1. ✅ Создать проект бэкенда (Ktor)
2. ✅ Настроить подключение к базе данных
3. ✅ Реализовать API endpoints:
   - `/api/v1/auth/register`
   - `/api/v1/auth/login`
   - `/api/v1/auth/reset-password`
4. ✅ Настроить JWT аутентификацию
5. ✅ Настроить nginx для проксирования запросов
6. ✅ Настроить SSL сертификат (Let's Encrypt)
7. ✅ Обновить `BASE_URL` в Android приложении

---

## 📊 Сравнение вариантов

| Критерий | Kotlin + Ktor | Node.js + Express | Python + FastAPI |
|---------|---------------|-------------------|------------------|
| **Язык** | Kotlin ✅ | JavaScript/TypeScript | Python |
| **Скорость разработки** | ⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ |
| **Производительность** | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐⭐ |
| **Экосистема** | ⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ |
| **Простота** | ⭐⭐⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ |
| **Единый язык с Android** | ✅ Да | ❌ Нет | ❌ Нет |

---

## ❓ Вопросы для выбора

1. **Хотите использовать один язык для Android и бэкенда?**
   - Да → **Kotlin + Ktor**
   - Нет → Node.js или Python

2. **Нужна максимальная скорость разработки?**
   - Да → **Node.js + Express** или **Python + FastAPI**
   - Нет → Kotlin + Ktor

3. **Нужна автоматическая документация API?**
   - Да → **Python + FastAPI**
   - Нет → Kotlin или Node.js

---

## ✅ Итоговая рекомендация

**Для вашего проекта рекомендую: Kotlin + Ktor**

**Что установить на сервере:**
1. ✅ Java (JDK 17+)
2. ✅ PostgreSQL
3. ✅ Настроить firewall

**После установки:**
- Создать проект Ktor
- Настроить базу данных
- Реализовать API endpoints

---

**Готов помочь с установкой и настройкой! Какой вариант выбираете?**
