# Руководство по добавлению проекта в GitHub

## 📋 Что нужно добавить в репозиторий

### ✅ Обязательные файлы (добавить)

#### 1. Исходный код
```
app/src/main/java/          # Весь исходный код Kotlin
app/src/test/java/          # Тесты
app/src/main/res/           # Ресурсы (drawable, values, xml и т.д.)
app/src/main/assets/        # Assets (JSON файлы маршрутов, аудио, изображения)
app/src/main/AndroidManifest.xml
```

#### 2. Конфигурационные файлы проекта
```
build.gradle.kts            # Корневой build.gradle
app/build.gradle.kts        # Build конфигурация приложения
settings.gradle.kts         # Настройки проекта
gradle.properties           # Свойства Gradle
gradle/                     # Gradle wrapper (gradlew, gradlew.bat, wrapper/)
.gitignore                  # Уже настроен правильно
```

#### 3. Документация проекта
```
README.md                   # Основная документация
DRAWER_IMPLEMENTATION.md    # Документация по реализации drawer
DRAWER_TEST_CASES.md        # Тесткейсы для drawer
АНАЛИЗ_ПРОЕКТА.md          # Анализ проекта
TESTING_REPORT.md           # Отчет о тестировании
TEST_REPORT.md              # Отчет о тестах
NEXT_STEPS.md               # План дальнейшей разработки
PROJECT_STATUS.md           # Статус проекта
QUICK_START.md              # Быстрый старт
RUNNING_INSTRUCTIONS.md     # Инструкции по запуску
```

#### 4. Документация по решению проблем
```
KAPT_URGENT_FIX.md          # Решение проблемы KAPT
KAPT_FINAL_FIX.md           # Финальное решение KAPT
FINAL_KAPT_SOLUTION.md      # Финальное решение KAPT
DEBUG_LOGCAT_ERRORS.md      # Отладка ошибок
DEBUG_CRASH.md              # Отладка крашей
```

#### 5. Инструкции и гайды
```
HOW_TO_ADD_ROUTE.md         # Как добавить маршрут
HOW_TO_GET_POLYLINE.md     # Как получить polyline
ROUTE_DATA_FORMAT.md        # Формат данных маршрута
ROUTE_PREPARATION_GUIDE.md  # Гайд по подготовке маршрута
SETUP_INSTRUCTIONS.md       # Инструкции по настройке
```

#### 6. Вспомогательные скрипты (опционально)
```
check_logs.ps1              # Скрипт проверки логов
check_logs_simple.ps1       # Упрощенный скрипт
open_in_android_studio.ps1  # Скрипт открытия в Android Studio
open_in_android_studio.bat  # Batch скрипт
```

#### 7. ProGuard правила
```
app/proguard-rules.pro      # Правила ProGuard
```

---

### ❌ Что НЕ нужно добавлять (уже в .gitignore)

Следующие файлы и папки **автоматически исключены** благодаря `.gitignore`:

```
build/                      # Скомпилированные файлы
app/build/                  # Скомпилированные файлы приложения
.gradle/                    # Кэш Gradle
local.properties            # Локальные настройки (путь к SDK)
google-services.json        # Firebase конфигурация (может содержать секреты)
*.apk                       # Скомпилированные APK файлы
*.aab                       # Android App Bundle
.idea/                      # Настройки Android Studio (частично)
*.iml                       # Файлы модулей IntelliJ
```

---

## 🔐 Важные замечания по безопасности

### ⚠️ Файлы, которые НЕ должны попасть в репозиторий:

1. **`google-services.json`** - уже в .gitignore ✅
   - Содержит конфигурацию Firebase
   - Может содержать чувствительные данные

2. **`local.properties`** - уже в .gitignore ✅
   - Содержит путь к Android SDK на вашем компьютере
   - Персональная информация

3. **Keystore файлы** (если есть)
   - Файлы `.jks` или `.keystore` для подписи приложения
   - **НИКОГДА** не добавляйте их в репозиторий!

4. **API ключи в коде**
   - Проверьте, нет ли хардкода API ключей в исходном коде
   - Используйте переменные окружения или `local.properties`

---

## 📝 Рекомендуемая структура README.md

Создайте или обновите `README.md` с следующей структурой:

```markdown
# Trust The Route

Мобильное приложение для навигации по туристическим маршрутам.

## Описание

Приложение позволяет пользователям просматривать туристические маршруты, 
отслеживать свое местоположение на карте и слушать аудиогиды о достопримечательностях.

## Технологии

- Kotlin
- Jetpack Compose
- MVVM Architecture
- Room Database
- Yandex MapKit
- Hilt (Dependency Injection)
- Retrofit
- ExoPlayer (Media3)

## Установка

1. Клонируйте репозиторий
2. Откройте проект в Android Studio
3. Создайте файл `local.properties` с путем к Android SDK
4. Создайте файл `app/google-services.json` (если используете Firebase)
5. Синхронизируйте проект с Gradle файлами
6. Запустите приложение

## Документация

- [Быстрый старт](QUICK_START.md)
- [Инструкции по запуску](RUNNING_INSTRUCTIONS.md)
- [Анализ проекта](АНАЛИЗ_ПРОЕКТА.md)
- [Статус проекта](PROJECT_STATUS.md)

## Известные проблемы

См. [АНАЛИЗ_ПРОЕКТА.md](АНАЛИЗ_ПРОЕКТА.md) для списка известных проблем и ограничений.

## Лицензия

[Укажите лицензию]
```

---

## 🚀 Пошаговая инструкция по добавлению в GitHub

### Шаг 1: Проверка .gitignore

Убедитесь, что `.gitignore` содержит все необходимые исключения (уже настроен правильно ✅).

### Шаг 2: Инициализация Git репозитория (если еще не сделано)

```bash
cd "C:\Trust The Route"
git init
```

### Шаг 3: Проверка статуса файлов

```bash
git status
```

Это покажет, какие файлы будут добавлены, а какие игнорируются.

### Шаг 4: Добавление файлов

```bash
# Добавить все файлы (кроме тех, что в .gitignore)
git add .

# Или добавить конкретные файлы
git add README.md
git add app/src/
git add build.gradle.kts
# и т.д.
```

### Шаг 5: Проверка перед коммитом

```bash
# Посмотреть, что будет добавлено
git status

# Убедитесь, что НЕТ следующих файлов:
# - local.properties
# - google-services.json
# - build/
# - app/build/
# - .gradle/
```

### Шаг 6: Первый коммит

```bash
git commit -m "Initial commit: Trust The Route mobile app"
```

### Шаг 7: Создание репозитория на GitHub

1. Зайдите на [GitHub.com](https://github.com)
2. Нажмите "New repository"
3. Укажите название (например, `trust-the-route`)
4. **НЕ** добавляйте README, .gitignore или лицензию (они уже есть)
5. Нажмите "Create repository"

### Шаг 8: Подключение к удаленному репозиторию

```bash
# Замените YOUR_USERNAME на ваш GitHub username
git remote add origin https://github.com/YOUR_USERNAME/trust-the-route.git

# Или через SSH
git remote add origin git@github.com:YOUR_USERNAME/trust-the-route.git
```

### Шаг 9: Отправка в GitHub

```bash
# Отправить код в репозиторий
git branch -M main
git push -u origin main
```

---

## 📊 Рекомендации по коммитам

Для демонстрации хода разработки рекомендуется делать коммиты с понятными сообщениями:

```bash
# Примеры хороших коммитов:
git commit -m "feat: Add drawer menu with clickable overlay"
git commit -m "fix: Resolve KAPT compilation issues with Java 17"
git commit -m "docs: Add drawer implementation documentation"
git commit -m "fix: Handle empty Flow in hasRoutes() method"
git commit -m "feat: Add error handling in YandexMapView"
```

Используйте префиксы:
- `feat:` - новая функциональность
- `fix:` - исправление бага
- `docs:` - изменения в документации
- `refactor:` - рефакторинг кода
- `test:` - добавление тестов

---

## 🔍 Проверка перед публикацией

Перед отправкой в GitHub проверьте:

- [ ] Нет файлов с секретами (API ключи, пароли)
- [ ] Нет `local.properties`
- [ ] Нет `google-services.json` (или он не содержит секретов)
- [ ] Нет скомпилированных файлов (`build/`, `app/build/`)
- [ ] Есть `README.md` с описанием проекта
- [ ] Есть `.gitignore` файл
- [ ] Все важные файлы документации добавлены

---

## 📁 Структура репозитория (пример)

```
trust-the-route/
├── .gitignore
├── README.md
├── build.gradle.kts
├── settings.gradle.kts
├── gradle.properties
├── gradle/
│   └── wrapper/
├── app/
│   ├── build.gradle.kts
│   ├── proguard-rules.pro
│   └── src/
│       ├── main/
│       │   ├── AndroidManifest.xml
│       │   ├── java/
│       │   ├── res/
│       │   └── assets/
│       └── test/
├── Документация/
│   ├── DRAWER_IMPLEMENTATION.md
│   ├── DRAWER_TEST_CASES.md
│   ├── АНАЛИЗ_ПРОЕКТА.md
│   └── ...
└── Скрипты/
    ├── check_logs.ps1
    └── ...
```

---

## 💡 Дополнительные рекомендации

### 1. Добавьте описание репозитория на GitHub
В настройках репозитория добавьте:
- Описание проекта
- Топики (topics): `android`, `kotlin`, `jetpack-compose`, `yandex-mapkit`
- Сайт проекта (если есть)

### 2. Создайте файл LICENSE
Если проект открытый, добавьте файл с лицензией (MIT, Apache 2.0 и т.д.)

### 3. Настройте GitHub Actions (опционально)
Можно добавить CI/CD для автоматической сборки и тестирования

### 4. Добавьте badges в README
```markdown
![Kotlin](https://img.shields.io/badge/Kotlin-1.9.0-blue)
![Android](https://img.shields.io/badge/Android-API%2026+-green)
![Compose](https://img.shields.io/badge/Compose-1.5.4-orange)
```

---

## ❓ Часто задаваемые вопросы

**Q: Нужно ли добавлять папку `.idea/`?**  
A: Нет, она частично исключена в `.gitignore`. Можно добавить только `.idea/codeStyles/` если хотите сохранить стиль кода команды.

**Q: Что делать с `google-services.json`?**  
A: Создайте файл `google-services.json.example` с примером структуры (без реальных значений) и добавьте его в репозиторий. В `.gitignore` оставьте `google-services.json`.

**Q: Нужно ли добавлять скрипты PowerShell?**  
A: Да, если они полезны для разработки. Они помогут другим разработчикам.

**Q: Как быть с большими медиа файлами (аудио, изображения)?**  
A: GitHub имеет ограничение на размер файлов (100 MB). Если файлы большие, рассмотрите использование Git LFS или хранение их отдельно.

---

## ✅ Финальный чеклист

Перед отправкой в GitHub убедитесь:

- [ ] `.gitignore` настроен правильно
- [ ] Нет секретов в коде
- [ ] `README.md` создан и заполнен
- [ ] Все важные файлы документации добавлены
- [ ] Исходный код добавлен
- [ ] Конфигурационные файлы добавлены
- [ ] Проверен статус через `git status`
- [ ] Создан первый коммит
- [ ] Репозиторий создан на GitHub
- [ ] Код отправлен в репозиторий

---

**Готово! Ваш проект теперь на GitHub и готов к демонстрации хода разработки! 🎉**
