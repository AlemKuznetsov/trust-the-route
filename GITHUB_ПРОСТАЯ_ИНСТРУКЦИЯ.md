# 📚 Простая инструкция: Как добавить проект в GitHub

Эта инструкция написана для тех, кто никогда не работал с GitHub. Мы пройдем все шаги от начала до конца.

---

## 🎯 Что такое GitHub?

GitHub - это сайт, где можно хранить код вашего проекта. Это как "облако" для программистов. Ваш дипломный проект будет храниться там, и вы сможете показать его преподавателям.

---

## 📋 Что нужно перед началом

1. **Учетная запись на GitHub**
   - Зайдите на [github.com](https://github.com)
   - Нажмите "Sign up" (Регистрация)
   - Создайте бесплатный аккаунт
   - Запомните ваш username (имя пользователя)

2. **Git установлен на компьютере**
   - Откройте PowerShell или командную строку
   - Введите: `git --version`
   - Если видите версию (например, `git version 2.40.0`) - всё готово ✅
   - Если ошибка - скачайте Git с [git-scm.com/download/win](https://git-scm.com/download/win) и установите

---

## 🚀 Пошаговая инструкция

### Шаг 1: Откройте PowerShell

1. Нажмите клавишу `Win + X`
2. Выберите "Windows PowerShell" или "Терминал"
3. Или найдите "PowerShell" в меню Пуск

### Шаг 2: Перейдите в папку проекта

В PowerShell введите:

```powershell
cd "C:\Trust The Route"
```

Нажмите Enter. Вы должны увидеть, что путь изменился.

### Шаг 3: Проверьте, что Git работает

Введите:

```powershell
git --version
```

Должна появиться версия Git. Если ошибка - установите Git (см. выше).

### Шаг 4: Настройте Git (если первый раз)

Если вы используете Git впервые, нужно указать ваше имя и email:

```powershell
git config --global user.name "Ваше Имя"
git config --global user.email "ваш_email@example.com"
```

**Важно:** Используйте тот же email, что и при регистрации на GitHub!

### Шаг 5: Инициализируйте Git репозиторий

Введите:

```powershell
git init
```

Вы увидите сообщение: `Initialized empty Git repository...`

### Шаг 6: Проверьте, какие файлы будут добавлены

Введите:

```powershell
git status
```

Вы увидите список файлов. Убедитесь, что **НЕТ** следующих файлов:
- ❌ `local.properties`
- ❌ `google-services.json` (если он содержит секреты)
- ❌ папки `build/` или `app/build/`

Эти файлы должны быть в `.gitignore` и не попадут в репозиторий.

### Шаг 7: Добавьте все файлы

Введите:

```powershell
git add .
```

Эта команда добавляет все файлы проекта (кроме тех, что в `.gitignore`).

### Шаг 8: Создайте первый коммит

Введите:

```powershell
git commit -m "Initial commit: Trust The Route mobile app"
```

Коммит - это "снимок" вашего проекта в определенный момент времени. Это как сохранение в игре.

### Шаг 9: Создайте репозиторий на GitHub

1. Откройте браузер и зайдите на [github.com](https://github.com)
2. Войдите в свой аккаунт
3. Нажмите кнопку **"+"** в правом верхнем углу
4. Выберите **"New repository"** (Новый репозиторий)

5. Заполните форму:
   - **Repository name:** `trust-the-route` (или другое название)
   - **Description:** `Мобильное приложение-гид для Android`
   - Выберите **Public** (публичный) или **Private** (приватный)
   - **ВАЖНО:** НЕ ставьте галочки на:
     - ❌ Add a README file
     - ❌ Add .gitignore
     - ❌ Choose a license
   
   (Эти файлы у вас уже есть!)

6. Нажмите **"Create repository"** (Создать репозиторий)

### Шаг 10: Подключите локальный проект к GitHub

После создания репозитория GitHub покажет вам инструкции. Найдите раздел **"...or push an existing repository from the command line"**

В PowerShell введите (замените `YOUR_USERNAME` на ваш GitHub username):

```powershell
git remote add origin https://github.com/YOUR_USERNAME/trust-the-route.git
```

**Пример:** Если ваш username `ivanov`, команда будет:
```powershell
git remote add origin https://github.com/ivanov/trust-the-route.git
```

### Шаг 11: Переименуйте ветку в main

Введите:

```powershell
git branch -M main
```

### Шаг 12: Отправьте код на GitHub

Введите:

```powershell
git push -u origin main
```

**Важно:** При первом запуске Git может попросить авторизацию:
- Введите ваш GitHub username
- Введите пароль (или Personal Access Token, если требуется)

Если всё прошло успешно, вы увидите сообщение о загрузке файлов.

### Шаг 13: Проверьте результат

1. Обновите страницу вашего репозитория на GitHub
2. Вы должны увидеть все файлы вашего проекта!

---

## ✅ Готово!

Ваш проект теперь на GitHub! Вы можете:
- Показать ссылку преподавателю
- Продолжать работать над проектом
- Делать изменения и отправлять их на GitHub

---

## 📝 Как добавлять изменения в будущем

Когда вы внесете изменения в проект, выполните:

```powershell
# 1. Перейти в папку проекта
cd "C:\Trust The Route"

# 2. Добавить изменения
git add .

# 3. Создать коммит с описанием изменений
git commit -m "Описание того, что вы изменили"

# 4. Отправить на GitHub
git push
```

**Примеры сообщений коммитов:**
- `git commit -m "Добавлен экран настроек"`
- `git commit -m "Исправлена ошибка с картой"`
- `git commit -m "Добавлена темная тема"`

---

## 🔧 Использование готового скрипта

В вашем проекте есть файл `add_to_github.ps1`, который автоматизирует некоторые шаги.

Чтобы использовать его:

1. Откройте PowerShell
2. Перейдите в папку проекта:
   ```powershell
   cd "C:\Trust The Route"
   ```
3. Запустите скрипт:
   ```powershell
   .\add_to_github.ps1
   ```

Скрипт выполнит шаги 5-8 автоматически, но вам всё равно нужно будет:
- Создать репозиторий на GitHub (Шаг 9)
- Подключить репозиторий (Шаг 10)
- Отправить код (Шаг 12)

---

## ❓ Частые проблемы и решения

### Проблема: "git: command not found"
**Решение:** Git не установлен. Скачайте с [git-scm.com/download/win](https://git-scm.com/download/win)

### Проблема: "Permission denied" при push
**Решение:** 
1. Проверьте правильность username и пароля
2. Если используете двухфакторную аутентификацию, создайте Personal Access Token:
   - GitHub → Settings → Developer settings → Personal access tokens → Tokens (classic)
   - Generate new token
   - Используйте токен вместо пароля

### Проблема: "Repository not found"
**Решение:** 
- Проверьте правильность username в команде `git remote add origin`
- Убедитесь, что репозиторий создан на GitHub

### Проблема: "fatal: not a git repository"
**Решение:** Вы не в папке проекта или не выполнили `git init`. Выполните:
```powershell
cd "C:\Trust The Route"
git init
```

### Проблема: Файлы не отправляются
**Решение:** 
- Убедитесь, что выполнили `git add .`
- Проверьте `git status` - файлы должны быть зелеными
- Убедитесь, что создали коммит: `git commit -m "сообщение"`

---

## 📚 Полезные команды Git

```powershell
# Посмотреть статус файлов
git status

# Посмотреть историю коммитов
git log

# Посмотреть, какие файлы изменены
git diff

# Отменить изменения в файле (до git add)
git checkout -- имя_файла

# Посмотреть подключенные репозитории
git remote -v
```

---

## 🎓 Для дипломного проекта

### Что важно для защиты диплома:

1. **Регулярные коммиты** - показывают ход работы
   - Делайте коммиты каждый раз, когда добавляете новую функцию
   - Пишите понятные сообщения коммитов

2. **README.md** - описание проекта
   - Убедитесь, что файл заполнен
   - Добавьте скриншоты приложения (если есть)

3. **История разработки**
   - GitHub автоматически сохраняет историю всех изменений
   - Преподаватель сможет увидеть, как развивался проект

4. **Ветки (branches)** - опционально
   - Можно создавать отдельные ветки для разных функций
   - Но для дипломного проекта достаточно одной ветки `main`

---

## 📞 Нужна помощь?

Если что-то не работает:
1. Перечитайте инструкцию
2. Проверьте раздел "Частые проблемы"
3. Посмотрите более подробную инструкцию в файле `GITHUB_SETUP.md`

---

**Удачи с дипломным проектом! 🎉**
