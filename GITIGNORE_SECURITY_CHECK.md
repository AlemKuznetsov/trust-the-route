# Проверка безопасности перед коммитом в GitHub

## ✅ Что уже в .gitignore

Следующие файлы и папки уже защищены:

- ✅ `local.properties` - путь к Android SDK (персональная информация)
- ✅ `google-services.json` - конфигурация Firebase (может содержать секреты)
- ✅ `build/` - скомпилированные файлы
- ✅ `.gradle/` - кэш Gradle
- ✅ `.idea/` - настройки Android Studio (частично)
- ✅ `*.apk`, `*.aab` - скомпилированные приложения
- ✅ `*.log` - логи
- ✅ `*.keystore`, `*.jks` - файлы подписи (если раскомментированы)

## ⚠️ Что нужно проверить

### 1. API ключи в коде

**Текущая ситуация:**

В коде захардкожены следующие ключи:

1. **Yandex MapKit API Key:** `ac4cbd52-7836-461c-8441-2295322b28eb`
   - Файлы: `AndroidManifest.xml`, `TrustTheRouteApplication.kt`
   - **Статус:** ⚠️ Это публичный ключ, но лучше вынести в BuildConfig

2. **Yandex OAuth Client ID:** `18401c8a769a4f5f9a546e05ea184851`
   - Файл: `YandexAuthManager.kt`
   - **Статус:** ✅ Для OAuth Client ID это нормально (он публичный, не секрет)

**Рекомендации:**

- ✅ **Yandex OAuth Client ID** - можно оставить в коде (это публичный идентификатор)
- ⚠️ **Yandex MapKit API Key** - лучше вынести в BuildConfig или local.properties
- ⚠️ **Client Secret** (если будет использоваться) - НИКОГДА не коммитить в код!

### 2. Дополнительные проверки

Проверьте, что нет:
- [ ] Паролей в коде
- [ ] Client Secret для OAuth
- [ ] Приватных ключей
- [ ] Токенов доступа
- [ ] Личных данных пользователей

## 🔒 Рекомендации по безопасности

### Для продакшена:

1. **Вынести API ключи в BuildConfig:**
   ```kotlin
   // В app/build.gradle.kts
   android {
       defaultConfig {
           buildConfigField("String", "YANDEX_MAPKIT_API_KEY", "\"${project.findProperty("YANDEX_MAPKIT_API_KEY") ?: ""}\"")
       }
   }
   ```

2. **Использовать local.properties:**
   ```properties
   YANDEX_MAPKIT_API_KEY=ваш_ключ_здесь
   YANDEX_CLIENT_ID=ваш_client_id_здесь
   ```

3. **Добавить в .gitignore:**
   - `secrets.properties` (если создадите)
   - `api-keys.properties` (если создадите)

### Текущий статус для коммита:

✅ **Можно коммитить:**
- Yandex OAuth Client ID в коде (это публичный идентификатор)
- Yandex MapKit API Key (если это тестовый/разработческий ключ)

⚠️ **Рекомендуется перед продакшеном:**
- Вынести все API ключи в BuildConfig/local.properties
- Использовать разные ключи для dev/staging/production

## 📝 Что добавить в .gitignore (опционально)

Если планируете использовать файлы для секретов:

```gitignore
# Secrets and API keys
secrets.properties
api-keys.properties
*.secrets
.env
```

**Примечание:** Эти файлы уже добавлены в .gitignore в обновленной версии.

---

## ✅ Итоговая проверка перед коммитом

- [ ] Проверил, что нет паролей в коде
- [ ] Проверил, что нет Client Secret
- [ ] Проверил, что `local.properties` в .gitignore
- [ ] Проверил, что `google-services.json` в .gitignore
- [ ] Проверил, что build папки в .gitignore
- [ ] Понимаю, что API ключи в коде будут видны в репозитории
- [ ] Готов коммитить с текущими настройками

---

**Дата проверки:** _______________  
**Проверил:** _______________
