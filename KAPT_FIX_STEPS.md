# Пошаговая инструкция: Исправление ошибки KAPT

## ⚠️ ВАЖНО: Обязательно выполните все шаги!

Если ошибка все еще появляется, это означает, что **Gradle daemon не перезапустился** и не подхватил новые настройки.

---

## ✅ Что уже исправлено:

1. ✅ **gradle.properties** - добавлены JVM аргументы в одну строку (для Windows)
2. ✅ **app/build.gradle.kts** - добавлена конфигурация KAPT с `--add-opens` аргументами

---

## 🔧 Обязательные шаги для применения исправлений:

### Шаг 1: Закройте Android Studio полностью
- **File → Exit** (или просто закройте окно)
- Убедитесь, что процесс Android Studio полностью завершен

### Шаг 2: Остановите все процессы Gradle daemon

**Способ 1: Через командную строку (если Gradle установлен глобально)**
```powershell
gradle --stop
```

**Способ 2: Убить процессы вручную (Windows)**
1. Нажмите **Ctrl + Shift + Esc** для открытия Диспетчера задач
2. Найдите процессы `java.exe` или `gradle.exe`
3. Завершите все процессы, связанные с Gradle

**Способ 3: Через PowerShell**
```powershell
Get-Process | Where-Object {$_.ProcessName -like "*java*" -or $_.ProcessName -like "*gradle*"} | Stop-Process -Force
```

### Шаг 3: Удалите кэш Gradle (опционально, но рекомендуется)

**Удалите папку:**
```
C:\Users\ВашеИмя\.gradle\daemon
```

Или через PowerShell:
```powershell
Remove-Item -Path "$env:USERPROFILE\.gradle\daemon" -Recurse -Force -ErrorAction SilentlyContinue
```

### Шаг 4: Откройте Android Studio заново

1. Запустите Android Studio
2. Откройте проект: **File → Open** → выберите папку проекта

### Шаг 5: Очистите кэш Android Studio

1. **File → Invalidate Caches / Restart**
2. Выберите **Invalidate and Restart**
3. Дождитесь перезапуска Android Studio

### Шаг 6: Синхронизируйте проект

1. **File → Sync Project with Gradle Files**
2. Или нажмите иконку слона в верхней панели
3. Дождитесь завершения синхронизации (может занять несколько минут)

### Шаг 7: Очистите и пересоберите проект

**Вариант 1 (рекомендуется):**
1. **Build → Clean Project**
2. Дождитесь завершения очистки
3. **Build → Assemble Project** - для полной сборки проекта
4. Дождитесь завершения сборки

**Вариант 2 (альтернатива):**
1. **Build → Clean Project**
2. Дождитесь завершения очистки
3. **Build → Make Project** (или нажмите **Ctrl+F9**) - для инкрементальной сборки
4. Дождитесь завершения сборки

**Вариант 3 (самый простой):**
1. **Build → Clean Project**
2. Дождитесь завершения очистки
3. Просто запустите приложение: **Run → Run 'app'** (или нажмите **Shift+F10**)
   - Это автоматически соберет проект перед запуском

**Примечание:** Опция "Rebuild Project" может отсутствовать в некоторых версиях Android Studio. Используйте **Assemble Project** или **Make Project** вместо неё.

---

## 🔍 Проверка, что исправления применены:

### Проверка 1: Убедитесь, что файлы изменены

**gradle.properties:**
```properties
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8 --add-opens=jdk.compiler/com.sun.tools.javac.api=ALL-UNNAMED ...
```

**app/build.gradle.kts:**
Должен содержать блок:
```kotlin
kapt {
    javacOptions {
        option("--add-opens", "jdk.compiler/com.sun.tools.javac.api=ALL-UNNAMED")
        // ... остальные опции
    }
}
```

### Проверка 2: Проверьте логи сборки

1. Откройте панель **Build** внизу Android Studio
2. При сборке не должно быть ошибок `IllegalAccessError`
3. Если ошибка все еще есть, перейдите к альтернативным решениям ниже

---

## 🆘 Если проблема все еще сохраняется:

### Альтернативное решение 1: Проверьте версию Java

Убедитесь, что используется Java 17:
```powershell
java -version
```

В Android Studio:
- **File → Settings → Build, Execution, Deployment → Build Tools → Gradle**
- Проверьте поле **Gradle JDK** - должно быть Java 17

### Альтернативное решение 2: Используйте Java 11 (временно)

Если ничего не помогает, можно временно использовать Java 11:
1. Установите Java 11
2. В Android Studio укажите Java 11 как Gradle JDK
3. Пересоберите проект

⚠️ **Внимание:** Это временное решение, рекомендуется использовать Java 17+ с исправлениями выше.

### Альтернативное решение 3: Миграция на KSP

KSP (Kotlin Symbol Processing) не имеет проблем с модульной системой Java:

1. Замените в `app/build.gradle.kts`:
   ```kotlin
   // Удалите:
   id("kotlin-kapt")
   
   // Добавьте:
   id("com.google.devtools.ksp") version "1.9.20-1.0.14"
   ```

2. Замените все `kapt` на `ksp` в зависимостях:
   ```kotlin
   // Было:
   kapt("com.google.dagger:hilt-android-compiler:2.48")
   kapt("androidx.room:room-compiler:2.6.1")
   
   // Стало:
   ksp("com.google.dagger:hilt-android-compiler:2.48")
   ksp("androidx.room:room-compiler:2.6.1")
   ```

3. Удалите блок `kapt { ... }` из `build.gradle.kts`

---

## 📝 Чеклист для проверки:

- [ ] Android Studio полностью закрыт
- [ ] Gradle daemon остановлен (`gradle --stop`)
- [ ] Кэш Gradle daemon удален (опционально)
- [ ] Android Studio перезапущен
- [ ] Кэш Android Studio очищен (Invalidate Caches)
- [ ] Проект синхронизирован
- [ ] Проект очищен (Clean Project)
- [ ] Проект собран (Assemble Project или Make Project)
- [ ] Ошибка больше не появляется

---

## 💡 Почему это важно?

Gradle daemon работает в фоновом режиме и кэширует настройки. Если не перезапустить daemon, он продолжит использовать старые настройки без `--add-opens` аргументов, и ошибка будет повторяться.

**Всегда перезапускайте Gradle daemon после изменения `gradle.properties`!**

---

Если после выполнения всех шагов проблема сохраняется, создайте issue с полным логом ошибки.
