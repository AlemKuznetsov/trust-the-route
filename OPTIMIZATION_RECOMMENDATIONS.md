# Рекомендации по оптимизации приложения

Данный документ содержит конкретные рекомендации по оптимизации мобильного приложения Trust The Route по различным аспектам: производительность, размер APK, использование памяти, батарея, сеть и UI.

---

## 📦 1. Оптимизация размера APK

### 1.1. Split APK по архитектурам
**Текущее состояние:** Используются все архитектуры (`x86_64`, `armeabi-v7a`, `arm64-v8a`)

**Рекомендация:**
```kotlin
android {
    splits {
        abi {
            isEnable = true
            reset()
            include("armeabi-v7a", "arm64-v8a") // Убираем x86_64 для production
            isUniversalApk = false
        }
    }
}
```

**Эффект:** Уменьшение размера APK на 30-40% (x86_64 нужен только для эмуляторов)

---

### 1.2. Оптимизация ресурсов
**Текущее состояние:** Все ресурсы включены в APK

**Рекомендации:**
- Использовать WebP вместо PNG для изображений (экономия 25-35%)
- Оптимизировать векторные drawables
- Удалить неиспользуемые ресурсы через `shrinkResources = true`

```kotlin
buildTypes {
    release {
        isMinifyEnabled = true
        isShrinkResources = true // Добавить
        proguardFiles(...)
    }
}
```

**Эффект:** Уменьшение размера APK на 15-25%

---

### 1.3. Оптимизация зависимостей
**Текущее состояние:** Используется полная версия Yandex MapKit (`4.5.1-full`)

**Рекомендация:**
- Использовать lite-версию MapKit, если не нужны все функции
- Исключить неиспользуемые модули библиотек

**Эффект:** Уменьшение размера APK на 5-10 MB

---

### 1.4. Bundle вместо APK
**Рекомендация:** Использовать Android App Bundle (AAB) для публикации в Google Play

**Преимущества:**
- Google Play автоматически создает оптимизированные APK для каждого устройства
- Уменьшение размера загрузки на 20-30%
- Динамическая доставка модулей

---

## 🚀 2. Оптимизация производительности

### 2.1. Оптимизация Compose UI

#### 2.1.1. Минимизация рекомпозиций
**Проблема:** Возможны лишние рекомпозиции при обновлении состояния

**Рекомендации:**

1. **Использовать `derivedStateOf` для производных состояний:**
```kotlin
val filteredRoutes = remember(routes, searchQuery) {
    derivedStateOf {
        if (searchQuery.isBlank()) routes
        else routes.filter { it.name.contains(searchQuery, ignoreCase = true) }
    }
}
```

2. **Оптимизация `LaunchedEffect`:**
```kotlin
// Вместо множественных LaunchedEffect использовать один с ключами
LaunchedEffect(key1 = route, key2 = attractions) {
    // Обновление карты
}
```

3. **Использовать `@Stable` и `@Immutable` аннотации:**
```kotlin
@Immutable
data class Route(...)

@Stable
class MapUiState(...)
```

**Эффект:** Уменьшение рекомпозиций на 30-50%

---

#### 2.1.2. Оптимизация списков
**Текущее состояние:** Используется `LazyRow` для изображений

**Рекомендации:**
```kotlin
LazyRow(
    items = imageUrls,
    key = { it }, // Добавить key для оптимизации
    contentType = { "image" } // Добавить contentType
) { imageUrl ->
    // ...
}
```

**Эффект:** Улучшение производительности прокрутки на 20-30%

---

### 2.2. Оптимизация работы с изображениями

#### 2.2.1. Кэширование изображений
**Текущее состояние:** Coil уже использует кэширование, но можно оптимизировать

**Рекомендации:**
```kotlin
AsyncImage(
    model = imageUrl,
    contentDescription = null,
    modifier = Modifier.size(200.dp),
    contentScale = ContentScale.Crop,
    // Добавить кэширование
    memoryCachePolicy = CachePolicy.ENABLED,
    diskCachePolicy = CachePolicy.ENABLED
)
```

---

#### 2.2.2. Оптимизация размера изображений
**Проблема:** Загружаются полноразмерные изображения

**Рекомендации:**
- Использовать `coil.compose.AsyncImage` с параметрами размера
- Предзагрузка изображений для следующего экрана
- Использование placeholder'ов для улучшения UX

```kotlin
AsyncImage(
    model = ImageRequest.Builder(context)
        .data(imageUrl)
        .size(Size(800, 600)) // Ограничить размер
        .memoryCachePolicy(CachePolicy.ENABLED)
        .build(),
    contentDescription = null,
    placeholder = painterResource(R.drawable.placeholder),
    error = painterResource(R.drawable.error_placeholder)
)
```

**Эффект:** Уменьшение использования памяти на 40-60%

---

### 2.3. Оптимизация работы с картой

#### 2.3.1. Кэширование тайлов карты
**Рекомендация:** Настроить кэширование тайлов Yandex MapKit

```kotlin
// В Application или MapScreen
MapKitFactory.getInstance().setMapTileCacheSize(50 * 1024 * 1024) // 50 MB
```

**Эффект:** Уменьшение использования сети и улучшение производительности

---

#### 2.3.2. Оптимизация маркеров
**Текущее состояние:** Маркеры создаются как Bitmap при каждом обновлении

**Рекомендация:** Кэшировать Bitmap маркеров

```kotlin
// Создать кэш для маркеров
private val markerCache = mutableMapOf<String, Bitmap>()

fun getCachedMarker(context: Context, type: String): Bitmap {
    return markerCache.getOrPut(type) {
        createMarkerBitmap(context, type)
    }
}
```

**Эффект:** Уменьшение времени создания маркеров на 50-70%

---

#### 2.3.3. Дебаунсинг обновлений карты
**Текущее состояние:** Обновления происходят при каждом изменении местоположения

**Рекомендация:** Дебаунсинг обновлений маркеров

```kotlin
var updateTrigger by remember { mutableStateOf(0) }

LaunchedEffect(currentLocation) {
    delay(500) // Дебаунс 500ms
    updateTrigger++
}
```

**Эффект:** Уменьшение нагрузки на карту на 30-40%

---

## 🔋 3. Оптимизация батареи

### 3.1. Оптимизация обновлений местоположения
**Текущее состояние:** Обновления каждые 2 секунды (`LOCATION_UPDATE_INTERVAL = 2000L`)

**Рекомендации:**

1. **Адаптивный интервал обновления:**
```kotlin
fun getLocationUpdates(intervalMillis: Long = Constants.LOCATION_UPDATE_INTERVAL): Flow<Location> {
    // Увеличить интервал, если пользователь не двигается
    val adaptiveInterval = if (isUserMoving()) {
        intervalMillis
    } else {
        intervalMillis * 5 // 10 секунд если не двигается
    }
    // ...
}
```

2. **Использовать `PRIORITY_BALANCED_POWER_ACCURACY` вместо `PRIORITY_HIGH_ACCURACY`:**
```kotlin
val locationRequest = LocationRequest.Builder(
    Priority.PRIORITY_BALANCED_POWER_ACCURACY, // Вместо HIGH_ACCURACY
    intervalMillis
)
```

**Эффект:** Уменьшение расхода батареи на 40-60%

---

### 3.2. Оптимизация фоновых задач
**Текущее состояние:** WorkManager используется для уведомлений

**Рекомендации:**
- Использовать `OneTimeWorkRequest` вместо периодических задач, где возможно
- Настроить ограничения для WorkManager (только при подключении к Wi-Fi, зарядке)

```kotlin
val constraints = Constraints.Builder()
    .setRequiredNetworkType(NetworkType.UNMETERED) // Только Wi-Fi
    .setRequiresCharging(false)
    .build()
```

**Эффект:** Уменьшение расхода батареи на 20-30%

---

### 3.3. Оптимизация аудиоплеера
**Текущее состояние:** ExoPlayer создается как Singleton

**Рекомендации:**
- Освобождать ресурсы при паузе на длительное время
- Использовать эквалайзер только при необходимости
- Оптимизировать буферизацию

```kotlin
fun pause() {
    if (player.isPlaying) {
        player.pause()
        // Освободить буфер после 30 секунд паузы
        viewModelScope.launch {
            delay(30_000)
            if (!player.isPlaying) {
                player.release()
            }
        }
    }
}
```

**Эффект:** Уменьшение расхода батареи на 10-15%

---

## 💾 4. Оптимизация использования памяти

### 4.1. Оптимизация работы с Bitmap
**Текущее состояние:** Bitmap создаются для маркеров каждый раз

**Проблема:** Утечки памяти при частом создании Bitmap

**Рекомендации:**

1. **Переиспользование Bitmap:**
```kotlin
object BitmapPool {
    private val markerPool = mutableMapOf<String, Bitmap>()
    
    fun getMarker(type: String, context: Context): Bitmap {
        return markerPool.getOrPut(type) {
            createMarkerBitmap(context, type)
        }
    }
    
    fun clear() {
        markerPool.values.forEach { it.recycle() }
        markerPool.clear()
    }
}
```

2. **Использование `Bitmap.Config.RGB_565` для маркеров:**
```kotlin
val bitmap = Bitmap.createBitmap(
    width, height,
    Bitmap.Config.RGB_565 // Вместо ARGB_8888 для экономии памяти
)
```

**Эффект:** Уменьшение использования памяти на 30-40%

---

### 4.2. Оптимизация базы данных

#### 4.2.1. Индексы для часто используемых запросов
**Рекомендация:** Добавить индексы в Room

```kotlin
@Entity(
    tableName = "attractions",
    indices = [
        Index(value = ["routeId"]), // Для getAttractionsByRouteId
        Index(value = ["latitude", "longitude"]) // Для поиска по местоположению
    ]
)
data class AttractionEntity(...)
```

**Эффект:** Ускорение запросов на 50-80%

---

#### 4.2.2. Пагинация для больших списков
**Текущее состояние:** Загружаются все достопримечательности сразу

**Рекомендация:** Использовать Paging 3 для списков

```kotlin
// Добавить зависимость
implementation("androidx.paging:paging-compose:3.2.1")
implementation("androidx.paging:paging-runtime-ktx:3.2.1")

// Использовать в DAO
@Query("SELECT * FROM attractions WHERE routeId = :routeId")
fun getAttractionsPaged(routeId: String): PagingSource<Int, AttractionEntity>
```

**Эффект:** Уменьшение использования памяти на 60-70% для больших списков

---

### 4.3. Оптимизация ViewModels
**Рекомендации:**

1. **Использовать `stateIn` с правильными параметрами:**
```kotlin
val routes: StateFlow<List<Route>> = routeRepository.getAllRoutes()
    .stateIn(
        scope = viewModelScope,
        started = SharingStarted.WhileSubscribed(5000), // Уже используется ✅
        initialValue = emptyList()
    )
```

2. **Кэширование данных:**
```kotlin
private var cachedRoutes: List<Route>? = null

fun getRoutes(): Flow<List<Route>> {
    if (cachedRoutes != null) {
        return flowOf(cachedRoutes!!)
    }
    return routeRepository.getAllRoutes().onEach { cachedRoutes = it }
}
```

**Эффект:** Уменьшение нагрузки на базу данных на 30-40%

---

## 🌐 5. Оптимизация сети

### 5.1. Кэширование HTTP запросов
**Текущее состояние:** OkHttp используется, но кэширование может быть не настроено

**Рекомендация:** Настроить кэш для OkHttp

```kotlin
@Provides
@Singleton
fun provideOkHttpClient(@ApplicationContext context: Context): OkHttpClient {
    val cacheSize = 50 * 1024 * 1024L // 50 MB
    val cache = Cache(context.cacheDir, cacheSize)
    
    return OkHttpClient.Builder()
        .cache(cache)
        .addInterceptor(HttpLoggingInterceptor().apply {
            level = if (BuildConfig.DEBUG) {
                HttpLoggingInterceptor.Level.BODY
            } else {
                HttpLoggingInterceptor.Level.NONE
            }
        })
        .build()
}
```

**Эффект:** Уменьшение использования сети на 40-60%

---

### 5.2. Оптимизация API запросов
**Рекомендации:**

1. **Использовать ETag для условных запросов:**
```kotlin
@Headers("Cache-Control: max-age=3600")
@GET("routes")
suspend fun getRoutes(): Response<List<RouteResponse>>
```

2. **Дебаунсинг поисковых запросов:**
```kotlin
fun onSearchQueryChange(query: String) {
    viewModelScope.launch {
        delay(300) // Дебаунс 300ms
        _searchQuery.value = query
    }
}
```

**Эффект:** Уменьшение количества запросов на 50-70%

---

### 5.3. Оптимизация загрузки медиа
**Рекомендации:**

1. **Предзагрузка аудио:**
```kotlin
fun preloadAudio(url: String) {
    viewModelScope.launch {
        audioPlayer.prepare(url) // Подготовить без воспроизведения
    }
}
```

2. **Сжатие аудио на сервере:**
- Использовать формат Opus вместо MP3 (меньший размер при том же качестве)
- Использовать битрейт 64-96 kbps для аудиогидов

**Эффект:** Уменьшение размера аудиофайлов на 30-50%

---

## 🎨 6. Оптимизация UI/UX

### 6.1. Улучшение времени загрузки
**Рекомендации:**

1. **Splash Screen с оптимизацией:**
```kotlin
// Использовать Splash Screen API для Android 12+
// Показывать контент как можно быстрее
```

2. **Ленивая загрузка данных:**
```kotlin
// Загружать маршруты только когда экран виден
LaunchedEffect(Unit) {
    if (isScreenVisible) {
        viewModel.loadRoutes()
    }
}
```

**Эффект:** Улучшение воспринимаемой производительности на 40-50%

---

### 6.2. Оптимизация анимаций
**Рекомендации:**

1. **Использовать `rememberInfiniteTransition` для повторяющихся анимаций:**
```kotlin
val infiniteTransition = rememberInfiniteTransition(label = "loading")
val alpha by infiniteTransition.animateFloat(
    initialValue = 0.3f,
    targetValue = 1f,
    animationSpec = infiniteRepeatable(
        animation = tween(1000, easing = LinearEasing),
        repeatMode = RepeatMode.Reverse
    ),
    label = "alpha"
)
```

2. **Отключать анимации на слабых устройствах:**
```kotlin
val isLowEndDevice = remember {
    ActivityManager.isLowRamDevice
}

val animationSpec = if (isLowEndDevice) {
    tween(0) // Без анимации
} else {
    tween(300)
}
```

**Эффект:** Улучшение производительности на слабых устройствах на 20-30%

---

## 📊 7. Мониторинг и профилирование

### 7.1. Инструменты для анализа
**Рекомендации:**

1. **Использовать Android Profiler:**
   - CPU Profiler для анализа производительности
   - Memory Profiler для поиска утечек памяти
   - Network Profiler для анализа сетевых запросов

2. **Добавить метрики производительности:**
```kotlin
class PerformanceTracker {
    fun trackScreenLoad(screenName: String, duration: Long) {
        // Отправка метрик в аналитику
    }
    
    fun trackMemoryUsage() {
        val runtime = Runtime.getRuntime()
        val usedMemory = runtime.totalMemory() - runtime.freeMemory()
        // Логирование или отправка метрик
    }
}
```

---

### 7.2. Логирование в production
**Текущее состояние:** Используется `android.util.Log` везде

**Рекомендация:** Отключать логи в release-сборках

```kotlin
object AppLog {
    private const val DEBUG = BuildConfig.DEBUG
    
    fun d(tag: String, message: String) {
        if (DEBUG) android.util.Log.d(tag, message)
    }
    
    fun e(tag: String, message: String, throwable: Throwable? = null) {
        if (DEBUG) android.util.Log.e(tag, message, throwable)
    }
}
```

**Эффект:** Улучшение производительности на 5-10%

---

## 🔒 8. Безопасность и оптимизация

### 8.1. Обфускация кода
**Текущее состояние:** ProGuard включен только для release

**Рекомендации:**

1. **Проверить правила ProGuard:**
```proguard
# Сохранить классы для отладки
-keep class com.trusttheroute.app.** { *; }

# Оптимизировать
-optimizationpasses 5
-dontusemixedcaseclassnames
-dontskipnonpubliclibraryclasses
```

2. **Использовать R8 вместо ProGuard (уже используется через Gradle)**

**Эффект:** Уменьшение размера APK на 10-15%

---

## 📋 9. Приоритетные оптимизации (Quick Wins)

### Высокий приоритет (быстрая реализация, большой эффект):

1. ✅ **Включить `shrinkResources`** - экономия 15-25% размера APK
2. ✅ **Оптимизировать интервал обновления местоположения** - экономия 40-60% батареи
3. ✅ **Кэшировать Bitmap маркеров** - улучшение производительности на 50-70%
4. ✅ **Добавить индексы в базу данных** - ускорение запросов на 50-80%
5. ✅ **Настроить HTTP кэш** - экономия трафика на 40-60%

### Средний приоритет:

6. ✅ **Оптимизировать Compose рекомпозиции** - улучшение производительности UI на 30-50%
7. ✅ **Использовать Split APK** - экономия 30-40% размера APK
8. ✅ **Оптимизировать размер изображений** - экономия памяти на 40-60%
9. ✅ **Дебаунсинг обновлений карты** - уменьшение нагрузки на 30-40%

### Низкий приоритет (требует больше работы):

10. ✅ **Реализовать пагинацию** - для больших списков
11. ✅ **Предзагрузка контента** - улучшение UX
12. ✅ **Адаптивные анимации** - для слабых устройств

---

## 📈 Ожидаемые результаты

После реализации всех оптимизаций высокого и среднего приоритета:

- **Размер APK:** Уменьшение на 40-50%
- **Использование памяти:** Уменьшение на 30-40%
- **Расход батареи:** Уменьшение на 40-60%
- **Производительность UI:** Улучшение на 30-50%
- **Использование сети:** Уменьшение на 40-60%
- **Время загрузки:** Улучшение на 20-30%

---

## 🔧 Инструменты для проверки оптимизаций

1. **Android Studio Profiler** - для анализа производительности
2. **APK Analyzer** - для анализа размера APK
3. **LeakCanary** - для поиска утечек памяти (можно добавить как зависимость)
4. **Battery Historian** - для анализа расхода батареи
5. **Network Profiler** - для анализа сетевых запросов

---

## 📝 Примечания

- Все оптимизации должны быть протестированы на реальных устройствах
- Рекомендуется внедрять оптимизации постепенно и измерять результаты
- Некоторые оптимизации могут требовать изменений в архитектуре
- Важно балансировать между оптимизацией и читаемостью кода

---

**Последнее обновление:** 2024
