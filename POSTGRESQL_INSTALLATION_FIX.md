# Исправление установки PostgreSQL

Решение проблемы "unknown user postgres" при установке PostgreSQL.

---

## 🔍 Проблема

При выполнении команды `sudo -u postgres psql` возникает ошибка:
```
sudo: unknown user postgres
```

**Причина:** PostgreSQL не установлен или установлен неправильно.

---

## ✅ Решение

### Шаг 1: Проверьте, установлен ли PostgreSQL

```bash
# Проверка статуса PostgreSQL
sudo systemctl status postgresql

# Или проверка наличия пакета
dpkg -l | grep postgresql
```

**Если PostgreSQL не установлен**, вы увидите сообщение об ошибке или пустой вывод.

---

### Шаг 2: Установите PostgreSQL

```bash
# Обновите список пакетов
sudo apt update

# Установите PostgreSQL
sudo apt install postgresql postgresql-contrib -y
```

**После установки** автоматически создастся пользователь `postgres`.

---

### Шаг 3: Проверьте установку

```bash
# Проверьте статус службы
sudo systemctl status postgresql

# Проверьте версию PostgreSQL
psql --version

# Проверьте, что пользователь postgres создан
id postgres
```

**Ожидаемый результат:**
- Служба PostgreSQL должна быть активна (active)
- Версия PostgreSQL должна отобразиться
- Пользователь `postgres` должен существовать

---

### Шаг 4: Войдите в PostgreSQL

После успешной установки выполните:

```bash
sudo -u postgres psql
```

**Если команда работает**, вы увидите приглашение PostgreSQL:
```
postgres=#
```

---

### Шаг 5: Создайте базу данных и пользователя

В PostgreSQL выполните следующие команды:

```sql
-- Создать базу данных
CREATE DATABASE trust_the_route;

-- Создать пользователя (замените 'ваш_пароль' на надежный пароль)
CREATE USER trust_user WITH PASSWORD 'ваш_надежный_пароль_здесь';

-- Дать права пользователю
GRANT ALL PRIVILEGES ON DATABASE trust_the_route TO trust_user;

-- Выйти из PostgreSQL
\q
```

---

## 🔧 Альтернативный способ (если sudo не работает)

Если `sudo -u postgres psql` не работает, попробуйте:

### Вариант 1: Переключиться на пользователя postgres

```bash
# Переключиться на пользователя postgres
sudo su - postgres

# Войти в PostgreSQL
psql

# Выполнить команды SQL (см. выше)
# ...

# Выйти из PostgreSQL
\q

# Вернуться к обычному пользователю
exit
```

### Вариант 2: Использовать psql напрямую

```bash
# Войти в PostgreSQL от имени текущего пользователя
psql -U postgres

# Или если нужно указать базу данных
psql -U postgres -d postgres
```

---

## 📋 Полная последовательность команд

Если PostgreSQL не установлен, выполните все команды подряд:

```bash
# 1. Обновить систему
sudo apt update

# 2. Установить PostgreSQL
sudo apt install postgresql postgresql-contrib -y

# 3. Запустить службу PostgreSQL
sudo systemctl start postgresql
sudo systemctl enable postgresql

# 4. Проверить статус
sudo systemctl status postgresql

# 5. Войти в PostgreSQL
sudo -u postgres psql
```

**В PostgreSQL выполните:**

```sql
CREATE DATABASE trust_the_route;
CREATE USER trust_user WITH PASSWORD 'ваш_надежный_пароль';
GRANT ALL PRIVILEGES ON DATABASE trust_the_route TO trust_user;
\q
```

---

## 🧪 Проверка подключения

После создания базы данных и пользователя проверьте подключение:

```bash
# Подключиться к базе данных от имени нового пользователя
psql -U trust_user -d trust_the_route -h localhost
```

**Если подключение успешно**, вы увидите приглашение:
```
trust_the_route=>
```

---

## ⚠️ Если проблемы с sudo

Если возникают проблемы с `sudo`, проверьте:

```bash
# Проверьте, что вы в группе sudo
groups

# Проверьте права sudo
sudo -v
```

**Если вы не в группе sudo**, добавьте себя:

```bash
# От имени root или другого пользователя с sudo правами
sudo usermod -aG sudo $USER
```

После этого нужно выйти и войти заново, чтобы изменения вступили в силу.

---

## ✅ После успешной установки

После успешной установки PostgreSQL вы сможете:

1. ✅ Создать базу данных для бэкенда
2. ✅ Создать пользователя для подключения
3. ✅ Настроить бэкенд для работы с базой данных

---

**Выполните команды по порядку и дайте знать, если возникнут проблемы!**
