# Руководство по использованию SSH ключей для Yandex Cloud

Инструкция по работе с SSH ключами, сгенерированными в Yandex Cloud.

---

## 📦 Что вы получили

В архиве два файла:

1. **`ssh-key-1769657037850`** — **приватный ключ** (секретный, не передавать никому!)
2. **`ssh-key-1769657037850.pub`** — **публичный ключ** (можно передавать, добавлять на сервер)

---

## 🔐 Важно: безопасность

### ⚠️ Приватный ключ (`ssh-key-1769657037850`):
- **НИКОГДА** не передавайте никому
- **НИКОГДА** не загружайте в репозиторий Git
- Храните в безопасном месте
- Это ваш "пароль" для доступа к серверу

### ✅ Публичный ключ (`ssh-key-1769657037850.pub`):
- Можно безопасно передавать
- Добавляется на сервер при создании ВМ
- Без приватного ключа бесполезен

---

## 📋 Шаг 1: Добавление ключа в форму создания ВМ

### В форме создания виртуальной машины:

1. Найдите раздел **"Доступ"**
2. Найдите поле **"SSH-ключ"**
3. Нажмите **"Добавить ключ"**
4. Откройте файл **`ssh-key-1769657037850.pub`** в текстовом редакторе
5. Скопируйте **ВЕСЬ** содержимое файла (обычно одна строка)
6. Вставьте в поле формы

**Пример содержимого `.pub` файла:**
```
ssh-rsa AAAAB3NzaC1yc2EAAAADAQABAAACAQC... (длинная строка)
```

**Важно:** Копируйте всю строку целиком, включая начало `ssh-rsa` и конец.

---

## 💻 Шаг 2: Сохранение приватного ключа на компьютере

### Windows:

1. **Создайте папку для SSH ключей:**
   ```powershell
   mkdir C:\Users\ВашеИмя\.ssh
   ```

2. **Скопируйте приватный ключ:**
   - Скопируйте файл `ssh-key-1769657037850` в папку `C:\Users\ВашеИмя\.ssh\`
   - Переименуйте его в `yandex_vm_key` (опционально, для удобства)

3. **Установите правильные права доступа:**
   ```powershell
   icacls C:\Users\ВашеИмя\.ssh\yandex_vm_key /inheritance:r
   icacls C:\Users\ВашеИмя\.ssh\yandex_vm_key /grant:r "%USERNAME%:R"
   ```

### Linux / macOS:

1. **Создайте папку для SSH ключей (если нет):**
   ```bash
   mkdir -p ~/.ssh
   chmod 700 ~/.ssh
   ```

2. **Скопируйте приватный ключ:**
   ```bash
   cp ssh-key-1769657037850 ~/.ssh/yandex_vm_key
   ```

3. **Установите правильные права доступа:**
   ```bash
   chmod 600 ~/.ssh/yandex_vm_key
   ```

**Важно:** Права доступа 600 обязательны для безопасности!

---

## 🔌 Шаг 3: Подключение к виртуальной машине

### После создания ВМ вы получите IP адрес

### Windows (через PowerShell или CMD):

```powershell
# Используйте полный путь к ключу
ssh -i C:\Users\ВашеИмя\.ssh\yandex_vm_key ubuntu@<IP-АДРЕС-ВМ>
```

### Windows (через PuTTY):

1. Скачайте и установите [PuTTY](https://www.putty.org/)
2. Запустите **PuTTYgen**
3. Нажмите **"Load"** и выберите файл `ssh-key-1769657037850`
4. Сохраните как `.ppk` файл
5. В PuTTY:
   - Host: `<IP-АДРЕС-ВМ>`
   - Port: `22`
   - Connection → SSH → Auth → Credentials
   - Выберите сохраненный `.ppk` файл

### Linux / macOS:

```bash
# Используйте ключ для подключения
ssh -i ~/.ssh/yandex_vm_key ubuntu@<IP-АДРЕС-ВМ>
```

**Или добавьте в SSH config для удобства:**

Создайте/отредактируйте файл `~/.ssh/config`:
```
Host yandex-vm
    HostName <IP-АДРЕС-ВМ>
    User ubuntu
    IdentityFile ~/.ssh/yandex_vm_key
    IdentitiesOnly yes
```

Теперь можно подключаться просто:
```bash
ssh yandex-vm
```

---

## 🔧 Шаг 4: Настройка SSH config (опционально, но удобно)

### Создайте файл `~/.ssh/config`:

**Windows:** `C:\Users\ВашеИмя\.ssh\config`  
**Linux/macOS:** `~/.ssh/config`

```
Host yandex-backend
    HostName <IP-АДРЕС-ВМ>
    User ubuntu
    IdentityFile ~/.ssh/yandex_vm_key
    IdentitiesOnly yes
    ServerAliveInterval 60
    ServerAliveCountMax 3
```

**Теперь можно подключаться:**
```bash
ssh yandex-backend
```

---

## ✅ Проверка подключения

### После подключения вы должны увидеть:

```
Welcome to Ubuntu 22.04 LTS (GNU/Linux ...)
...
ubuntu@compute-vm-2-2-20-ssd-...:~$
```

### Первые команды после подключения:

```bash
# Обновить систему
sudo apt update && sudo apt upgrade -y

# Проверить доступ к интернету
ping -c 3 google.com

# Проверить дисковое пространство
df -h

# Проверить память
free -h
```

---

## 🛠️ Решение проблем

### Проблема: "Permission denied (publickey)"

**Решение:**
1. Проверьте, что публичный ключ правильно добавлен в форму ВМ
2. Проверьте права доступа на приватный ключ (должны быть 600)
3. Убедитесь, что используете правильный пользователь (`ubuntu` для Ubuntu)

### Проблема: "Could not resolve hostname"

**Решение:**
1. Проверьте правильность IP адреса
2. Убедитесь, что ВМ запущена
3. Проверьте, что публичный IP назначен

### Проблема: "Connection timed out"

**Решение:**
1. Проверьте группу безопасности ВМ
2. Убедитесь, что порт 22 (SSH) открыт
3. Проверьте firewall на ВМ

---

## 🔒 Дополнительная безопасность

### После первого подключения:

1. **Отключите вход по паролю (если включен):**
   ```bash
   sudo nano /etc/ssh/sshd_config
   ```
   
   Найдите и измените:
   ```
   PasswordAuthentication no
   PubkeyAuthentication yes
   ```
   
   Перезапустите SSH:
   ```bash
   sudo systemctl restart sshd
   ```

2. **Настройте firewall:**
   ```bash
   sudo ufw allow 22/tcp    # SSH
   sudo ufw allow 80/tcp    # HTTP
   sudo ufw allow 443/tcp   # HTTPS
   sudo ufw enable
   ```

---

## 📝 Чеклист

- [ ] Публичный ключ (`.pub`) добавлен в форму создания ВМ
- [ ] Приватный ключ сохранен в `~/.ssh/` (или `C:\Users\...\.ssh\`)
- [ ] Установлены правильные права доступа (600)
- [ ] ВМ создана и запущена
- [ ] Получен IP адрес ВМ
- [ ] Подключение по SSH работает
- [ ] Настроен SSH config (опционально)

---

## 💡 Советы

1. **Резервная копия ключа:**
   - Сохраните приватный ключ в безопасном месте (например, зашифрованный архив)
   - Если потеряете ключ, доступ к ВМ будет невозможен

2. **Используйте разные ключи:**
   - Один ключ для одной ВМ
   - Не используйте один ключ для всех серверов

3. **Используйте SSH agent:**
   ```bash
   # Linux/macOS
   eval $(ssh-agent)
   ssh-add ~/.ssh/yandex_vm_key
   ```

---

## 🚀 Следующие шаги

После успешного подключения:

1. Обновите систему
2. Установите необходимое ПО (Node.js/Python/JDK)
3. Настройте firewall
4. Разверните бэкенд API

---

**Последнее обновление:** 2024
