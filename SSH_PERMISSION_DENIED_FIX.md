# Исправление: "Permission denied (publickey)"

Решение проблемы, когда SSH соединение устанавливается, но аутентификация не проходит.

---

## 🔍 Симптомы

При попытке подключения вы видите:
```
ubuntu@158.160.180.232: Permission denied (publickey).
```

или

```
admin_t_t_r@158.160.180.232: Permission denied (publickey).
```

**Что это означает:**
- ✅ Соединение установлено успешно
- ✅ Сервер доступен и отвечает
- ❌ Публичный ключ не найден на сервере или не соответствует приватному ключу

---

## 🎯 Главная причина

**Публичный ключ не был добавлен в VM при создании**, или был добавлен неправильный ключ.

---

## ✅ Решение

### Вариант 1: Добавить ключ через консоль Yandex Cloud (РЕКОМЕНДУЕТСЯ)

#### Шаг 1: Откройте содержимое публичного ключа

```powershell
# Покажите содержимое публичного ключа
Get-Content C:\Users\kuzne\.ssh\ssh-key-1769657037850.pub
```

Скопируйте весь вывод (он должен начинаться с `ssh-rsa` или `ssh-ed25519`).

#### Шаг 2: Добавьте ключ в консоли Yandex Cloud

1. Откройте консоль Yandex Cloud: https://console.cloud.yandex.ru/
2. Перейдите в **Compute Cloud** → **Виртуальные машины**
3. Найдите вашу VM (с IP `158.160.180.232`)
4. Нажмите на имя VM, чтобы открыть детали
5. Перейдите на вкладку **Метаданные** или **SSH-ключи**
6. Нажмите **Добавить SSH-ключ** или **Редактировать**
7. Вставьте содержимое публичного ключа (весь текст из шага 1)
8. Сохраните изменения

#### Шаг 3: Подождите 1-2 минуты

После добавления ключа подождите немного, чтобы изменения применились.

#### Шаг 4: Попробуйте подключиться снова

```powershell
# С пользователем ubuntu
ssh -i C:\Users\kuzne\.ssh\ssh-key-1769657037850 ubuntu@158.160.180.232

# Или с пользователем admin_t_t_r
ssh -i C:\Users\kuzne\.ssh\ssh-key-1769657037850 admin_t_t_r@158.160.180.232
```

---

### Вариант 2: Пересоздать VM с правильным ключом

Если VM только что создана и нет важных данных:

#### Шаг 1: Удалите текущую VM

1. В консоли Yandex Cloud найдите вашу VM
2. Нажмите **Остановить** (если запущена)
3. Нажмите **Удалить**

#### Шаг 2: Создайте новую VM

1. Нажмите **Создать виртуальную машину**
2. Заполните все необходимые поля
3. **ВАЖНО:** В разделе **Доступ** → **SSH-ключ**:
   - Откройте файл `C:\Users\kuzne\.ssh\ssh-key-1769657037850.pub`
   - Скопируйте **весь** его содержимое
   - Вставьте в поле **SSH-ключ** в форме создания VM
4. Создайте VM

#### Шаг 3: Подключитесь к новой VM

После создания VM получите новый IP адрес и подключитесь:

```powershell
ssh -i C:\Users\kuzne\.ssh\ssh-key-1769657037850 ubuntu@<новый-ip-адрес>
```

---

### Вариант 3: Использовать подключение через браузер (временное решение)

Если нужно срочно подключиться:

1. В консоли Yandex Cloud найдите вашу VM
2. Нажмите **Подключиться** → **Через браузер**
3. Откроется терминал в браузере
4. Через этот терминал можно добавить ваш публичный ключ вручную

---

## 🔍 Диагностика

### Шаг 1: Проверьте содержимое публичного ключа

```powershell
# Покажите публичный ключ
Get-Content C:\Users\kuzne\.ssh\ssh-key-1769657037850.pub
```

**Должно выглядеть примерно так:**
```
ssh-rsa AAAAB3NzaC1yc2EAAAADAQABAAACAQ... ваш_email@example.com
```

или

```
ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAI... ваш_email@example.com
```

**Важно:** Это должен быть **один длинный текст** (обычно 1-2 строки).

### Шаг 2: Проверьте формат приватного ключа

```powershell
# Покажите первую строку приватного ключа
Get-Content C:\Users\kuzne\.ssh\ssh-key-1769657037850 -First 1
```

**Должно быть:**
```
-----BEGIN OPENSSH PRIVATE KEY-----
```

или

```
-----BEGIN RSA PRIVATE KEY-----
```

### Шаг 3: Проверьте права доступа на ключ (Windows)

```powershell
# Установите правильные права доступа
icacls C:\Users\kuzne\.ssh\ssh-key-1769657037850 /inheritance:r
icacls C:\Users\kuzne\.ssh\ssh-key-1769657037850 /grant:r "%USERNAME%:R"
```

### Шаг 4: Попробуйте подключиться с подробным выводом

```powershell
# Подключение с подробным выводом
ssh -v -i C:\Users\kuzne\.ssh\ssh-key-1769657037850 ubuntu@158.160.180.232
```

Это покажет подробную информацию о процессе аутентификации.

---

## 📋 Проверка в консоли Yandex Cloud

### Как проверить, добавлен ли ключ:

1. Откройте консоль Yandex Cloud
2. Перейдите в **Compute Cloud** → **Виртуальные машины**
3. Найдите вашу VM (`158.160.180.232`)
4. Откройте детали VM
5. Перейдите на вкладку **Метаданные** или **SSH-ключи**
6. Проверьте список SSH-ключей

**Если ключа нет** — добавьте его (см. Вариант 1 выше).

**Если ключ есть, но не работает** — возможно, он был добавлен неправильно. Удалите его и добавьте заново.

---

## 🆘 Альтернативные решения

### Решение 1: Использовать WSL (Windows Subsystem for Linux)

Если у вас установлен WSL:

```bash
# В WSL терминале
ssh -i ~/.ssh/ssh-key-1769657037850 ubuntu@158.160.180.232
```

### Решение 2: Использовать PuTTY

1. Скачайте **PuTTY** и **PuTTYgen** с https://www.putty.org/
2. Откройте **PuTTYgen**
3. Нажмите **Load** и выберите ваш приватный ключ (`ssh-key-1769657037850`)
4. Сохраните ключ в формате `.ppk`
5. Откройте **PuTTY**:
   - **Host Name:** `158.160.180.232`
   - **Port:** `22`
   - **Connection type:** SSH
   - **Auth** → **Credentials** → **Private key file:** выберите сохраненный `.ppk` файл
6. Нажмите **Open**

### Решение 3: Использовать пароль (если был указан при создании)

Если при создании VM вы указали пароль:

```powershell
# Подключение с паролем (менее безопасно)
ssh ubuntu@158.160.180.232
# Введите пароль, когда попросит
```

---

## ✅ Правильная команда (после добавления ключа)

```powershell
# С пользователем ubuntu
ssh -i C:\Users\kuzne\.ssh\ssh-key-1769657037850 ubuntu@158.160.180.232

# Или с пользователем admin_t_t_r (если он указан в метаданных)
ssh -i C:\Users\kuzne\.ssh\ssh-key-1769657037850 admin_t_t_r@158.160.180.232
```

---

## 📝 Чеклист для проверки

- [ ] Публичный ключ (`ssh-key-1769657037850.pub`) существует
- [ ] Публичный ключ начинается с `ssh-rsa` или `ssh-ed25519`
- [ ] Приватный ключ (`ssh-key-1769657037850`) существует
- [ ] Приватный ключ начинается с `-----BEGIN`
- [ ] Публичный ключ был добавлен в VM через консоль Yandex Cloud
- [ ] Права доступа на приватный ключ установлены правильно
- [ ] Используется правильная команда с флагом `-i`

---

## 🎯 Рекомендуемый порядок действий

1. **Проверьте содержимое публичного ключа:**
   ```powershell
   Get-Content C:\Users\kuzne\.ssh\ssh-key-1769657037850.pub
   ```

2. **Скопируйте весь текст публичного ключа**

3. **Добавьте ключ в консоли Yandex Cloud:**
   - Compute Cloud → Виртуальные машины → Ваша VM → Метаданные → SSH-ключи
   - Добавьте скопированный ключ

4. **Подождите 1-2 минуты**

5. **Попробуйте подключиться:**
   ```powershell
   ssh -i C:\Users\kuzne\.ssh\ssh-key-1769657037850 ubuntu@158.160.180.232
   ```

---

**После добавления ключа через консоль Yandex Cloud подключение должно работать!**
