# Детальная диагностика SSH подключения

Пошаговая диагностика проблемы "Permission denied (publickey)".

---

## 🔍 Шаг 1: Подключение с подробным выводом

Выполните команду с флагом `-v` (verbose) для подробной диагностики:

```powershell
ssh -v -i C:\Users\kuzne\.ssh\ssh-key-1769657037850 ubuntu@158.160.180.232
```

**Что искать в выводе:**
- Строки с `Offering public key`
- Строки с `Server accepts key`
- Строки с `Permission denied`

**Скопируйте весь вывод** и отправьте мне — это поможет точно определить проблему.

---

## 🔍 Шаг 2: Проверка прав доступа на ключ

В Windows SSH может требовать правильные права доступа на приватный ключ:

```powershell
# Установите правильные права доступа
icacls C:\Users\kuzne\.ssh\ssh-key-1769657037850 /inheritance:r
icacls C:\Users\kuzne\.ssh\ssh-key-1769657037850 /grant:r "%USERNAME%:R"
```

После этого попробуйте подключиться снова.

---

## 🔍 Шаг 3: Проверка формата ключа

Убедитесь, что используете правильный файл:

```powershell
# Проверьте первую строку приватного ключа
Get-Content C:\Users\kuzne\.ssh\ssh-key-1769657037850 -First 1
```

**Должно быть:**
```
-----BEGIN OPENSSH PRIVATE KEY-----
```

или

```
-----BEGIN RSA PRIVATE KEY-----
```

**Если начинается с `ssh-rsa` или `ssh-ed25519`** — это публичный ключ, а не приватный!

---

## 🔍 Шаг 4: Проверка публичного ключа

Проверьте формат публичного ключа:

```powershell
# Покажите публичный ключ
Get-Content C:\Users\kuzne\.ssh\ssh-key-1769657037850.pub
```

**Должно быть:**
- Одна строка (или две, если очень длинный)
- Начинается с `ssh-rsa` или `ssh-ed25519`
- Нет лишних пробелов в начале или конце
- Нет лишних переносов строк

**Важно:** При копировании в консоль Yandex Cloud:
- Скопируйте **весь** ключ целиком
- Не добавляйте лишних пробелов
- Не добавляйте переносов строк (если ключ в одну строку)

---

## 🔍 Шаг 5: Проверка пользователя в консоли Yandex Cloud

В консоли Yandex Cloud проверьте:

1. Откройте вашу VM (`158.160.180.232`)
2. Перейдите в **Метаданные** → **Пользователи**
3. Посмотрите, какой пользователь указан

**Возможные варианты:**
- `ubuntu` (стандартный для Ubuntu)
- `admin_t_t_r` (специальный пользователь, созданный Yandex Cloud)
- Другой пользователь

Попробуйте подключиться с тем пользователем, который указан в метаданных.

---

## 🔍 Шаг 6: Попробуйте разные варианты подключения

### Вариант 1: С пользователем ubuntu
```powershell
ssh -i C:\Users\kuzne\.ssh\ssh-key-1769657037850 ubuntu@158.160.180.232
```

### Вариант 2: С пользователем admin_t_t_r
```powershell
ssh -i C:\Users\kuzne\.ssh\ssh-key-1769657037850 admin_t_t_r@158.160.180.232
```

### Вариант 3: С указанием типа ключа
```powershell
ssh -i C:\Users\kuzne\.ssh\ssh-key-1769657037850 -o PubkeyAcceptedKeyTypes=+ssh-rsa ubuntu@158.160.180.232
```

---

## 🔍 Шаг 7: Проверка в консоли Yandex Cloud

### Убедитесь, что ключ добавлен правильно:

1. Откройте консоль Yandex Cloud
2. Перейдите в **Compute Cloud** → **Виртуальные машины**
3. Найдите вашу VM
4. Откройте **Метаданные** → **SSH-ключи**
5. Проверьте список ключей

**Что проверить:**
- Ключ должен быть в списке
- Формат ключа должен быть правильным (начинается с `ssh-rsa` или `ssh-ed25519`)
- Не должно быть лишних пробелов или символов

**Если ключа нет или он неправильный:**
- Удалите старый ключ (если есть)
- Добавьте ключ заново, скопировав его из PowerShell

---

## 🔍 Шаг 8: Попробуйте через WSL (если установлен)

Если у вас установлен WSL (Windows Subsystem for Linux):

```bash
# В WSL терминале
ssh -i ~/.ssh/ssh-key-1769657037850 ubuntu@158.160.180.232
```

---

## 🔍 Шаг 9: Использование PuTTY (альтернатива)

Если стандартный SSH не работает, попробуйте PuTTY:

1. Скачайте **PuTTY** и **PuTTYgen** с https://www.putty.org/
2. Откройте **PuTTYgen**
3. Нажмите **Load** → выберите `C:\Users\kuzne\.ssh\ssh-key-1769657037850`
4. Сохраните ключ в формате `.ppk`
5. Откройте **PuTTY**:
   - **Host Name:** `158.160.180.232`
   - **Port:** `22`
   - **Connection type:** SSH
   - **Auth** → **Credentials** → **Private key file:** выберите `.ppk` файл
6. Нажмите **Open**

---

## 🆘 Если ничего не помогает

### Вариант 1: Пересоздать VM с правильным ключом

Если VM только что создана и нет важных данных:

1. **Удалите текущую VM** в консоли Yandex Cloud
2. **Создайте новую VM**
3. **При создании** в разделе **Доступ** → **SSH-ключ**:
   - Откройте файл `C:\Users\kuzne\.ssh\ssh-key-1769657037850.pub` в блокноте
   - Скопируйте **весь** его содержимое (Ctrl+A, Ctrl+C)
   - Вставьте в поле **SSH-ключ** в форме создания VM
   - **ВАЖНО:** Убедитесь, что скопировали весь ключ без лишних пробелов

### Вариант 2: Использовать подключение через браузер

1. В консоли Yandex Cloud найдите вашу VM
2. Нажмите **Подключиться** → **Через браузер**
3. Откроется терминал в браузере
4. Через этот терминал можно добавить ваш публичный ключ вручную:

```bash
# В терминале браузера выполните:
mkdir -p ~/.ssh
chmod 700 ~/.ssh
echo "ваш_публичный_ключ_здесь" >> ~/.ssh/authorized_keys
chmod 600 ~/.ssh/authorized_keys
```

---

## 📋 Чеклист для проверки

- [ ] Выполнена команда с `-v` для диагностики
- [ ] Установлены правильные права доступа на ключ (`icacls`)
- [ ] Проверен формат приватного ключа (начинается с `-----BEGIN`)
- [ ] Проверен формат публичного ключа (начинается с `ssh-rsa` или `ssh-ed25519`)
- [ ] Публичный ключ добавлен в консоль Yandex Cloud
- [ ] Проверен пользователь в метаданных VM
- [ ] Попробованы разные варианты подключения
- [ ] Проверено, что ключ правильно отображается в консоли Yandex Cloud

---

**Выполните команду с `-v` и отправьте мне вывод — это поможет точно определить проблему!**
