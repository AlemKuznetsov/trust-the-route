# Использование VPS для бэкенда Trust The Route

## ✅ Да, VPS отлично подходит!

VPS (Virtual Private Server) — это отличный выбор для размещения бэкенда вашего приложения. Это дает вам полный контроль над сервером и обычно дешевле, чем облачные платформы.

---

## 🎯 Преимущества VPS

### ✅ Контроль
- Полный контроль над сервером
- Можно установить любые технологии
- Гибкая настройка

### ✅ Стоимость
- Обычно дешевле облачных платформ
- Фиксированная цена (или по использованию)
- Нет скрытых платежей

### ✅ Производительность
- Выделенные ресурсы
- Предсказуемая производительность
- Можно масштабировать при необходимости

### ✅ Гибкость
- Можно использовать любую ОС (Linux, Windows)
- Можно установить любые технологии
- Легко настроить под свои нужды

---

## 🏆 Рекомендуемые VPS провайдеры

### 1. **Yandex Cloud** ⭐ РЕКОМЕНДУЮ (для России)

**Почему подходит:**
- ✅ Российский провайдер (быстрая скорость в России)
- ✅ Интеграция с Yandex IAM
- ✅ Хорошая документация на русском
- ✅ Бесплатный период для новых пользователей

**Цены:**
- От ~500₽/месяц за базовый VPS
- Есть бесплатный период

**Ссылка:** https://cloud.yandex.ru/services/compute

**Характеристики базового VPS:**
- 2 vCPU
- 4 GB RAM
- 20 GB SSD
- Достаточно для начала

---

### 2. **DigitalOcean** ⭐ ПОПУЛЯРНЫЙ

**Почему подходит:**
- ✅ Очень популярный и надежный
- ✅ Отличная документация
- ✅ Простая настройка
- ✅ Хорошая производительность

**Цены:**
- От $6/месяц (~600₽)
- Есть бесплатный кредит для новых пользователей

**Ссылка:** https://www.digitalocean.com/

**Характеристики базового VPS:**
- 1 vCPU
- 1 GB RAM
- 25 GB SSD
- Достаточно для начала

---

### 3. **Hetzner** ⭐ ДЕШЕВЫЙ

**Почему подходит:**
- ✅ Очень дешевые цены
- ✅ Хорошая производительность
- ✅ Надежность

**Цены:**
- От €4/месяц (~400₽)
- Очень выгодно

**Ссылка:** https://www.hetzner.com/

---

### 4. **Timeweb** ⭐ РОССИЙСКИЙ

**Почему подходит:**
- ✅ Российский провайдер
- ✅ Русскоязычная поддержка
- ✅ Хорошие цены

**Цены:**
- От ~300₽/месяц
- Очень доступно

**Ссылка:** https://timeweb.com/

---

### 5. **AWS Lightsail** ⭐ ДЛЯ МАСШТАБИРОВАНИЯ

**Почему подходит:**
- ✅ От Amazon (надежность)
- ✅ Легко масштабировать
- ✅ Интеграция с другими AWS сервисами

**Цены:**
- От $3.50/месяц (~350₽)
- Есть бесплатный период

**Ссылка:** https://aws.amazon.com/lightsail/

---

## 💻 Рекомендуемые характеристики VPS

### Для начала (MVP):
```
CPU: 1-2 vCPU
RAM: 1-2 GB
Диск: 20-40 GB SSD
Сеть: 1 TB трафика/месяц
```

### Для продакшена:
```
CPU: 2-4 vCPU
RAM: 4-8 GB
Диск: 40-80 GB SSD
Сеть: Безлимит или 2+ TB
```

---

## 🚀 Настройка бэкенда на VPS

### Шаг 1: Выбрать ОС

**Рекомендую: Ubuntu Server 22.04 LTS**
- ✅ Бесплатная
- ✅ Популярная и хорошо документированная
- ✅ Много готовых решений
- ✅ Стабильная

---

### Шаг 2: Установить необходимые компоненты

#### Для Kotlin + Ktor:

```bash
# Обновление системы
sudo apt update && sudo apt upgrade -y

# Установка Java (для Kotlin)
sudo apt install openjdk-17-jdk -y

# Установка PostgreSQL (база данных)
sudo apt install postgresql postgresql-contrib -y

# Установка Nginx (веб-сервер, опционально)
sudo apt install nginx -y

# Установка Git
sudo apt install git -y
```

#### Для Node.js + Express:

```bash
# Установка Node.js
curl -fsSL https://deb.nodesource.com/setup_20.x | sudo -E bash -
sudo apt install -y nodejs

# Установка PostgreSQL
sudo apt install postgresql postgresql-contrib -y

# Установка PM2 (для управления процессами)
sudo npm install -g pm2
```

#### Для Python + FastAPI:

```bash
# Установка Python
sudo apt install python3 python3-pip -y

# Установка PostgreSQL
sudo apt install postgresql postgresql-contrib -y

# Установка Gunicorn (веб-сервер)
pip3 install gunicorn
```

---

### Шаг 3: Настроить базу данных

```bash
# Войти в PostgreSQL
sudo -u postgres psql

# Создать базу данных
CREATE DATABASE trusttheroute;

# Создать пользователя
CREATE USER app_user WITH PASSWORD 'your_secure_password';

# Выдать права
GRANT ALL PRIVILEGES ON DATABASE trusttheroute TO app_user;

# Выйти
\q
```

---

### Шаг 4: Развернуть приложение

#### Вариант A: Через Git

```bash
# Клонировать репозиторий
git clone https://github.com/yourusername/trusttheroute-backend.git
cd trusttheroute-backend

# Установить зависимости и запустить
# (зависит от технологии)
```

#### Вариант B: Через Docker (рекомендую)

```bash
# Установить Docker
sudo apt install docker.io docker-compose -y

# Запустить через docker-compose
docker-compose up -d
```

---

### Шаг 5: Настроить Nginx (опционально)

```nginx
# /etc/nginx/sites-available/trusttheroute
server {
    listen 80;
    server_name api.trusttheroute.com;

    location / {
        proxy_pass http://localhost:8080;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
```

---

### Шаг 6: Настроить SSL (HTTPS)

```bash
# Установить Certbot
sudo apt install certbot python3-certbot-nginx -y

# Получить сертификат
sudo certbot --nginx -d api.trusttheroute.com
```

---

## 🔒 Безопасность VPS

### Обязательные шаги:

1. **Настроить firewall:**
```bash
sudo ufw allow 22/tcp  # SSH
sudo ufw allow 80/tcp  # HTTP
sudo ufw allow 443/tcp # HTTPS
sudo ufw enable
```

2. **Настроить SSH ключи:**
```bash
# На локальной машине
ssh-keygen -t rsa

# Скопировать на сервер
ssh-copy-id user@your-server-ip
```

3. **Отключить вход по паролю:**
```bash
# В /etc/ssh/sshd_config
PasswordAuthentication no
```

4. **Регулярно обновлять:**
```bash
sudo apt update && sudo apt upgrade -y
```

---

## 📊 Мониторинг и управление

### Полезные инструменты:

1. **htop** - мониторинг ресурсов
```bash
sudo apt install htop -y
htop
```

2. **PM2** (для Node.js) - управление процессами
```bash
pm2 start app.js
pm2 monit
```

3. **Docker** - контейнеризация
```bash
docker ps
docker logs container_name
```

---

## 💰 Примерная стоимость

### Минимальная конфигурация:
- **Yandex Cloud:** ~500₽/месяц
- **DigitalOcean:** ~600₽/месяц ($6)
- **Hetzner:** ~400₽/месяц (€4)
- **Timeweb:** ~300₽/месяц

### Для продакшена:
- **Yandex Cloud:** ~1500-3000₽/месяц
- **DigitalOcean:** ~2000₽/месяц ($20)
- **Hetzner:** ~1000₽/месяц (€10)

---

## 🎯 Рекомендация для вашего проекта

### Для начала (MVP):
**Yandex Cloud или Timeweb**
- Российские провайдеры (быстрая скорость)
- Хорошие цены
- Русскоязычная поддержка

**Характеристики:**
- 2 vCPU
- 2 GB RAM
- 40 GB SSD
- ~500-800₽/месяц

### Для продакшена:
**Yandex Cloud или DigitalOcean**
- Надежность
- Хорошая производительность
- Легко масштабировать

**Характеристики:**
- 4 vCPU
- 4-8 GB RAM
- 80 GB SSD
- ~2000-3000₽/месяц

---

## 📝 Чеклист настройки VPS

- [ ] Выбрать провайдера
- [ ] Создать VPS (Ubuntu 22.04)
- [ ] Настроить SSH доступ
- [ ] Установить необходимые компоненты
- [ ] Настроить базу данных
- [ ] Развернуть приложение
- [ ] Настроить Nginx (опционально)
- [ ] Настроить SSL (HTTPS)
- [ ] Настроить firewall
- [ ] Настроить мониторинг
- [ ] Настроить резервное копирование

---

## 🚀 Следующие шаги

1. **Выбрать провайдера VPS**
2. **Создать VPS** (Ubuntu 22.04)
3. **Настроить сервер** (по инструкции выше)
4. **Развернуть бэкенд** (Kotlin + Ktor или другой)
5. **Обновить BASE_URL** в Android приложении

---

## ❓ Вопросы

**Нужна ли помощь с:**
- [ ] Выбором провайдера?
- [ ] Настройкой VPS?
- [ ] Развертыванием бэкенда?
- [ ] Настройкой базы данных?

---

**Вывод:** VPS — отличный выбор! Рекомендую начать с Yandex Cloud или Timeweb для российского проекта.
