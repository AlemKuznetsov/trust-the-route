# Руководство по настройке Yandex Cloud для бэкенда

Пошаговая инструкция по созданию необходимых ресурсов в Yandex Cloud для бэкенда приложения.

---

## 🎯 Что нужно создать

Для полноценного бэкенда с регистрацией/авторизацией нужно:

1. ✅ **Виртуальная машина** (VPS) — для бэкенд API
2. ✅ **База данных** (PostgreSQL) — для хранения пользователей
3. ✅ **Object Storage** — для медиафайлов (уже обсуждали)

---

## 📋 Шаг 1: Создание виртуальной машины (VPS)

### Что выбрать на первом скриншоте:

**✅ Выберите: "Виртуальная машина"**

**Почему:**
- Это сервер для размещения вашего бэкенд API
- Имеет свои вычислительные ресурсы, диски и сетевые интерфейсы
- Можно установить любой софт (Node.js, Python, Kotlin/Ktor и т.д.)

**Что НЕ выбирать:**
- ❌ Диск — это только хранилище, не сервер
- ❌ Файловое хранилище — для общих файлов между ВМ, не для бэкенда
- ❌ Группа виртуальных машин — для масштабирования, избыточно для начала

---

## ⚙️ Настройка виртуальной машины

### Рекомендуемые параметры для начала:

**Операционная система:**
- Ubuntu 22.04 LTS (рекомендуется)
- Или Ubuntu 20.04 LTS

**Ресурсы:**
- **CPU:** 2 ядра (достаточно для начала)
- **RAM:** 4 GB (можно начать с 2 GB, но 4 GB комфортнее)
- **Диск:** 20-30 GB SSD (достаточно для ОС и приложения)

**Сеть:**
- Публичный IP адрес — **обязательно** (для доступа из интернета)
- Группа безопасности — разрешить порты:
  - 80 (HTTP)
  - 443 (HTTPS)
  - 22 (SSH для управления)

**Стоимость:** ~500-1000₽/месяц (зависит от конфигурации)

---

## 📋 Шаг 2: Создание базы данных

### Где найти:

В главном меню Yandex Cloud найдите:
**"База данных" → "PostgreSQL" → "Создать кластер"**

### Рекомендуемые параметры:

**Версия:** PostgreSQL 14 или 15

**Ресурсы:**
- **CPU:** 2 ядра
- **RAM:** 4 GB
- **Диск:** 20 GB SSD (можно увеличить позже)

**Доступ:**
- Создайте пользователя и пароль
- Разрешите доступ с вашей ВМ (по IP)

**Стоимость:** ~800-1500₽/месяц

---

## 📋 Шаг 3: Object Storage (для медиафайлов)

### Где найти:

В главном меню Yandex Cloud:
**"Object Storage" → "Создать bucket"**

### Настройка:

- **Имя:** `trust-the-route-media`
- **Тип доступа:** Публичный (для статических файлов)
- **Класс хранилища:** Standard

**Стоимость:** ~30-50₽/месяц для 5 GB

---

## 🏗️ Итоговая архитектура

```
┌─────────────────────────────────────────┐
│         Yandex Cloud                     │
│                                          │
│  ┌──────────────────┐                   │
│  │  Виртуальная     │                   │
│  │  машина (VPS)    │                   │
│  │                  │                   │
│  │  Backend API     │                   │
│  │  (Kotlin/Ktor)   │                   │
│  └────────┬─────────┘                   │
│           │                              │
│           ▼                              │
│  ┌──────────────────┐                   │
│  │  PostgreSQL      │                   │
│  │  Database        │                   │
│  └──────────────────┘                   │
│                                          │
│  ┌──────────────────┐                   │
│  │  Object Storage  │                   │
│  │  (медиафайлы)    │                   │
│  └──────────────────┘                   │
└─────────────────────────────────────────┘
```

---

## 💰 Примерная стоимость

### Минимальная конфигурация:

- **Виртуальная машина:** ~500-800₽/месяц
  - 2 CPU, 2-4 GB RAM, 20 GB диск
- **PostgreSQL:** ~800-1200₽/месяц
  - 2 CPU, 4 GB RAM, 20 GB диск
- **Object Storage:** ~30-50₽/месяц
  - 5 GB хранения + трафик

**Итого: ~1300-2050₽/месяц**

### Можно оптимизировать:

- Использовать одну ВМ с установленной PostgreSQL (экономия ~800₽)
- Начать с меньших ресурсов и масштабировать при росте нагрузки

---

## 🚀 План действий

### Этап 1: Создание ВМ (сейчас)

1. Выберите **"Виртуальная машина"**
2. Настройте параметры (Ubuntu, 2 CPU, 4 GB RAM)
3. Создайте ВМ
4. Настройте SSH доступ

### Этап 2: Установка бэкенда

1. Подключитесь к ВМ по SSH
2. Установите необходимый софт (Node.js/Python/Kotlin)
3. Разверните бэкенд API
4. Настройте домен и SSL сертификат

### Этап 3: База данных

1. Создайте PostgreSQL кластер
2. Подключите к бэкенду
3. Выполните миграции

### Этап 4: Object Storage

1. Создайте bucket
2. Загрузите медиафайлы
3. Обновите URL в приложении

---

## ⚠️ Важные замечания

### Безопасность:

1. **SSH ключи:** Используйте SSH ключи вместо паролей
2. **Firewall:** Настройте группу безопасности (только нужные порты)
3. **SSL:** Обязательно используйте HTTPS (Let's Encrypt бесплатно)
4. **База данных:** Не открывайте PostgreSQL в интернет, только с ВМ

### Резервное копирование:

1. Настройте автоматические бэкапы базы данных
2. Регулярно делайте snapshot ВМ
3. Храните бэкапы в Object Storage

---

## 📝 Альтернативные варианты

### Если бюджет ограничен:

**Вариант 1: Одна ВМ с PostgreSQL**
- Установить PostgreSQL прямо на ВМ
- Экономия ~800₽/месяц
- Меньше отказоустойчивости

**Вариант 2: Serverless (Yandex Cloud Functions)**
- Для простых API endpoints
- Платите только за использование
- Может быть дешевле для небольших нагрузок

**Вариант 3: Другой VPS провайдер**
- Hetzner, DigitalOcean (может быть дешевле)
- Подключить к Yandex Object Storage

---

## ✅ Итоговая рекомендация

**Для начала выберите:**

1. ✅ **"Виртуальная машина"** — для бэкенд API
2. ✅ **PostgreSQL кластер** (отдельно) — для базы данных
3. ✅ **Object Storage** (отдельно) — для медиафайлов

**Минимальная конфигурация ВМ:**
- Ubuntu 22.04
- 2 CPU
- 4 GB RAM
- 20 GB SSD
- Публичный IP

Это позволит запустить бэкенд и протестировать регистрацию/авторизацию.

---

**Последнее обновление:** 2024
