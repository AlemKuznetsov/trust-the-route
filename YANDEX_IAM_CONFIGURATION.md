# Конфигурация Yandex IAM для приложения

## 📋 Текущий статус

✅ **Создано:**
- `AuthErrorResponse.kt` - обработка ошибок API
- Улучшен `AuthRepository.kt` с обработкой ошибок
- `YandexIamApi.kt` - интерфейс для работы с Yandex Cloud IAM (опционально)

## 🔧 Что нужно настроить

### Шаг 1: Определить BASE_URL API

**Вариант A: Собственный бэкенд с Yandex IAM**

Если у вас есть собственный бэкенд, который использует Yandex Cloud IAM:

1. Откройте файл: `app/src/main/java/com/trusttheroute/app/di/NetworkModule.kt`
2. Найдите строку:
   ```kotlin
   private const val BASE_URL = "https://api.trusttheroute.com/api/v1/"
   ```
3. Замените на ваш реальный URL API

**Пример:**
```kotlin
private const val BASE_URL = "https://your-backend.com/api/v1/"
```

---

**Вариант B: Прямое использование Yandex Cloud API**

Если вы хотите напрямую использовать Yandex Cloud IAM API:

1. Откройте файл: `app/src/main/java/com/trusttheroute/app/di/NetworkModule.kt`
2. Измените BASE_URL на:
   ```kotlin
   private const val BASE_URL = "https://iam.api.cloud.yandex.net/"
   ```
3. Используйте `YandexIamApi` вместо `AuthApi` (требует дополнительной настройки)

---

### Шаг 2: Настроить endpoints в AuthApi

Откройте файл: `app/src/main/java/com/trusttheroute/app/data/api/AuthApi.kt`

**Текущие endpoints:**
```kotlin
@POST("auth/register")
suspend fun register(@Body request: RegisterRequest): Response<AuthResponse>

@POST("auth/login")
suspend fun login(@Body request: LoginRequest): Response<AuthResponse>

@POST("auth/reset-password")
suspend fun resetPassword(@Body request: ResetPasswordRequest): Response<Unit>
```

**Если ваши endpoints другие, измените пути:**
```kotlin
@POST("users/register")  // Пример другого пути
suspend fun register(@Body request: RegisterRequest): Response<AuthResponse>
```

---

### Шаг 3: Проверить формат ответов API

**Ожидаемый формат ответа при регистрации/входе:**
```json
{
  "user": {
    "id": "user-id",
    "email": "user@example.com",
    "name": "User Name"
  },
  "token": "jwt-token-here"
}
```

**Если формат другой, нужно обновить:**
- `AuthResponse.kt` - структуру ответа
- `User.kt` - модель пользователя

---

### Шаг 4: Настроить обработку ошибок

**Текущая обработка ошибок поддерживает:**
- ✅ Код 400 - Неверный формат данных
- ✅ Код 401 - Неверные учетные данные
- ✅ Код 409 - Email уже существует
- ✅ Код 422 - Пароль не соответствует требованиям
- ✅ Код 500+ - Ошибка сервера
- ✅ Ошибки сети

**Если ваш API возвращает ошибки в другом формате, обновите:**
- `AuthErrorResponse.kt` - структуру ошибки
- Метод `parseError()` в `AuthRepository.kt`

---

## 🔑 Получение API ключей (если нужно)

### Для Yandex Cloud IAM:

1. Зайдите в [Yandex Cloud Console](https://console.cloud.yandex.ru/)
2. Создайте Service Account
3. Выдайте права на IAM
4. Создайте API ключ для Service Account
5. Сохраните ключ в безопасном месте (НЕ коммитьте в репозиторий!)

### Для собственного бэкенда:

1. Получите API ключи от разработчика бэкенда
2. Сохраните их в `local.properties` или используйте переменные окружения

---

## 📝 Пример настройки local.properties

Создайте файл `local.properties` в корне проекта (если его нет):

```properties
# API Configuration
API_BASE_URL=https://your-backend.com/api/v1/
API_KEY=your-api-key-here
```

**ВАЖНО:** Файл `local.properties` уже должен быть в `.gitignore`!

---

## 🧪 Тестирование подключения

После настройки можно протестировать:

1. Запустите приложение
2. Попробуйте зарегистрироваться
3. Проверьте Logcat на наличие ошибок
4. Если есть ошибки - проверьте:
   - Правильность BASE_URL
   - Правильность endpoints
   - Формат запросов/ответов
   - Наличие интернет-соединения

---

## ❓ Вопросы для уточнения

**Перед продолжением разработки нужно уточнить:**

1. ✅ **BASE_URL API** - какой URL использовать?
2. ✅ **Endpoints** - правильные ли пути (`/auth/register`, `/auth/login`)?
3. ✅ **Формат ответов** - совпадает ли с ожидаемым?
4. ✅ **API ключи** - нужны ли они и где их получить?
5. ✅ **Аутентификация** - нужен ли токен в заголовках запросов?

---

## 🚀 Следующие шаги

После настройки BASE_URL и endpoints:

1. ✅ Протестировать подключение к API
2. ✅ Реализовать UI экранов (Этап 1.2)
3. ✅ Создать AuthViewModel (Этап 1.3)
4. ✅ Интегрировать с UI (Этап 1.4)

---

**Статус:** Ожидание настройки BASE_URL и endpoints
