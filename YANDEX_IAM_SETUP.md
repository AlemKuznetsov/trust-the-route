# Настройка Yandex IAM для аутентификации

## 📋 Что такое Yandex IAM?

Yandex IAM (Identity and Access Management) — это сервис Yandex Cloud для управления доступом к ресурсам. Для аутентификации пользователей в мобильном приложении нам нужно использовать Yandex Cloud API или создать собственный бэкенд, который будет работать с Yandex IAM.

## ⚠️ Важное замечание

Yandex IAM в основном предназначен для управления доступом к ресурсам облака, а не для аутентификации пользователей мобильных приложений. 

**Рекомендуемые варианты:**
1. **Yandex ID (OAuth)** — для входа через аккаунт Yandex
2. **Собственный бэкенд с Yandex Cloud** — бэкенд использует Yandex IAM для управления пользователями
3. **Yandex Cloud API** — прямое использование API для создания пользователей

## 🔧 Вариант 1: Собственный бэкенд с Yandex Cloud IAM

Если у вас есть собственный бэкенд, который использует Yandex Cloud IAM:

### Шаги настройки:

1. **Получить API ключи от бэкенда:**
   - Base URL API (например: `https://api.trusttheroute.com/api/v1/`)
   - Endpoints для регистрации/входа
   - Формат запросов/ответов

2. **Настроить в приложении:**
   - Обновить `BASE_URL` в `NetworkModule.kt`
   - Обновить endpoints в `AuthApi.kt`
   - Настроить обработку токенов

## 🔧 Вариант 2: Yandex ID (OAuth)

Если вы хотите использовать вход через аккаунт Yandex:

### Требования:
- Yandex OAuth приложение
- Client ID и Client Secret
- Redirect URI

### Документация:
- https://yandex.ru/dev/id/doc/ru/

## 🔧 Вариант 3: Прямое использование Yandex Cloud API

Если вы хотите напрямую использовать Yandex Cloud API:

### Требования:
- Аккаунт Yandex Cloud
- Service Account с правами на IAM
- API ключ или OAuth токен

### Документация:
- https://cloud.yandex.ru/docs/iam/api-ref/

---

## 📝 Что нужно уточнить:

1. **Есть ли у вас бэкенд?**
   - Если да → какой URL и формат API?
   - Если нет → нужно создать или использовать другой вариант

2. **Какой вариант аутентификации предпочтительнее?**
   - Собственный бэкенд
   - Yandex ID (OAuth)
   - Прямое использование Yandex Cloud API

3. **Есть ли API ключи?**
   - Если да → предоставьте их (не коммитьте в репозиторий!)
   - Если нет → нужно создать

---

## 🚀 Следующие шаги:

После выбора варианта мы:
1. Настроим зависимости в `build.gradle.kts`
2. Обновим `NetworkModule.kt` с правильным BASE_URL
3. Обновим `AuthApi.kt` с правильными endpoints
4. Настроим обработку токенов и ошибок
