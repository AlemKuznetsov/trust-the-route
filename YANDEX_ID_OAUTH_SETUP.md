# Настройка Yandex ID OAuth для авторизации

## 📋 Обзор

Приложение использует **Yandex ID (OAuth)** для авторизации пользователей. Это позволяет пользователям входить в приложение через свой аккаунт Yandex.

## 🔧 Шаг 1: Регистрация приложения в Yandex OAuth

1. Перейдите на [Yandex OAuth](https://oauth.yandex.com/)
2. Войдите в свой аккаунт Yandex
3. Нажмите "Создать новое приложение"
4. Заполните форму:
   - **Название приложения**: Trust The Route
   - **Описание**: Приложение для навигации по маршрутам
   - **Платформы**: Android
   - **Redirect URI**: `trusttheroute://oauth/yandex`
   - **Разрешения (scopes)**:
     - `login:email` - доступ к email
     - `login:avatar` - доступ к аватару
     - `login:name` - доступ к имени пользователя

5. Сохраните приложение и скопируйте **Client ID**

## 🔑 Шаг 2: Настройка Client ID в приложении

После получения Client ID, нужно добавить его в код приложения:

1. Откройте файл: `app/src/main/java/com/trusttheroute/app/data/auth/YandexAuthManager.kt`
2. Найдите строку:
   ```kotlin
   private const val YANDEX_CLIENT_ID = "YOUR_YANDEX_CLIENT_ID_HERE"
   ```
3. Замените `YOUR_YANDEX_CLIENT_ID_HERE` на ваш реальный Client ID

**⚠️ ВАЖНО:** Для продакшена рекомендуется хранить Client ID в `local.properties` или использовать BuildConfig.

### Альтернативный способ (рекомендуется):

1. Создайте файл `local.properties` в корне проекта (если его нет)
2. Добавьте строку:
   ```properties
   YANDEX_CLIENT_ID=ваш_client_id_здесь
   ```
3. Обновите `YandexAuthManager.kt` для чтения из `local.properties`:
   ```kotlin
   private val YANDEX_CLIENT_ID = BuildConfig.YANDEX_CLIENT_ID
   ```
4. Добавьте в `app/build.gradle.kts`:
   ```kotlin
   android {
       defaultConfig {
           buildConfigField("String", "YANDEX_CLIENT_ID", "\"${project.findProperty("YANDEX_CLIENT_ID") ?: ""}\"")
       }
   }
   ```

## ✅ Шаг 3: Проверка настройки

После настройки Client ID:

1. Убедитесь, что Redirect URI в настройках приложения Yandex OAuth совпадает с указанным в коде: `trusttheroute://oauth/yandex`
2. Проверьте, что в `AndroidManifest.xml` зарегистрирована `YandexOAuthActivity` с правильным intent-filter
3. Запустите приложение и попробуйте войти через Yandex ID

## 🧪 Тестирование

1. Запустите приложение
2. На экране входа нажмите "Войти через Yandex ID"
3. Должен открыться браузер с формой авторизации Yandex
4. После успешной авторизации вы должны вернуться в приложение
5. Проверьте, что пользователь авторизован и данные сохранены

## 📝 Структура файлов

- `YandexAuthManager.kt` - менеджер для работы с OAuth
- `YandexOAuthActivity.kt` - Activity для обработки OAuth redirect
- `AuthViewModel.kt` - ViewModel для управления состоянием авторизации
- `LoginScreen.kt` - экран входа с кнопкой "Войти через Yandex ID"

## 🔒 Безопасность

- **НЕ коммитьте** Client ID в репозиторий
- Используйте `local.properties` или переменные окружения для хранения секретов
- Проверяйте `state` параметр для защиты от CSRF атак (уже реализовано)
- Используйте HTTPS для всех запросов к API Yandex

## ❓ Часто задаваемые вопросы

### Q: Что делать, если авторизация не работает?

A: Проверьте:
1. Правильность Client ID
2. Совпадение Redirect URI в настройках Yandex OAuth и в коде
3. Наличие интернет-соединения
4. Логи в Logcat для деталей ошибки

### Q: Можно ли использовать другой Redirect URI?

A: Да, но нужно:
1. Изменить URI в настройках приложения Yandex OAuth
2. Обновить `REDIRECT_URI` в `YandexAuthManager.kt`
3. Обновить intent-filter в `AndroidManifest.xml`

### Q: Как добавить дополнительные разрешения (scopes)?

A: Обновите `SCOPES` в `YandexAuthManager.kt` и добавьте их в настройках приложения Yandex OAuth.

## 📚 Дополнительные ресурсы

- [Документация Yandex ID](https://yandex.ru/dev/id/doc/ru/)
- [Yandex OAuth API](https://yandex.ru/dev/id/doc/ru/access)
- [OAuth 2.0 спецификация](https://oauth.net/2/)

---

**Статус:** ✅ Реализовано  
**Дата:** 2024
