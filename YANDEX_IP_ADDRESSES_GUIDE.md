# Руководство по IP адресам в Yandex Cloud

Объяснение разницы между внутренним и публичным IP адресами и когда какой использовать.

---

## 🌐 Типы IP адресов

### 1. Публичный IP адрес (Public IP)

**Что это:**
- IP адрес, доступный из интернета
- Можно подключиться с любого компьютера в мире
- Используется для доступа к серверу извне

**Пример:**
```
51.250.123.45
```

**Когда использовать:**
- ✅ SSH подключение с вашего компьютера
- ✅ Доступ к API из мобильного приложения
- ✅ Доступ к веб-интерфейсу из браузера
- ✅ Любой доступ из интернета

---

### 2. Внутренний IP адрес (Private IP)

**Что это:**
- IP адрес внутри сети Yandex Cloud
- Доступен только между ресурсами в одном облаке
- Недоступен из интернета

**Пример:**
```
10.128.0.5
```

**Когда использовать:**
- ✅ Подключение между ВМ в одном облаке
- ✅ Подключение ВМ к базе данных в том же облаке
- ✅ Внутренняя коммуникация между сервисами
- ❌ НЕ для подключения с вашего компьютера

---

## 🔌 Какой IP использовать для SSH

### ✅ Используйте: **Публичный IP адрес**

**Почему:**
- Ваш компьютер находится вне Yandex Cloud
- Нужен доступ через интернет
- Внутренний IP недоступен извне

**Команда подключения:**
```bash
ssh -i ~/.ssh/yandex_vm_key ubuntu@<ПУБЛИЧНЫЙ-IP>
```

**Пример:**
```bash
ssh -i ~/.ssh/yandex_vm_key ubuntu@51.250.123.45
```

---

## 📋 Примеры использования

### Сценарий 1: SSH подключение с вашего компьютера

**Используйте:** Публичный IP

```bash
# С вашего компьютера (Windows/Linux/macOS)
ssh -i ~/.ssh/yandex_vm_key ubuntu@51.250.123.45
```

---

### Сценарий 2: Подключение между ВМ в Yandex Cloud

**Используйте:** Внутренний IP

```bash
# С одной ВМ к другой ВМ в том же облаке
ssh ubuntu@10.128.0.5
```

**Преимущества:**
- Быстрее (внутренняя сеть)
- Бесплатно (нет платы за трафик)
- Безопаснее (не выходит в интернет)

---

### Сценарий 3: Подключение бэкенда к базе данных

**Используйте:** Внутренний IP базы данных

```kotlin
// В конфигурации бэкенда
database {
    host = "10.128.0.10"  // Внутренний IP PostgreSQL
    port = 5432
}
```

**Почему:**
- Быстрее и безопаснее
- Не нужно открывать базу данных в интернет
- Бесплатный трафик внутри облака

---

### Сценарий 4: Доступ к API из мобильного приложения

**Используйте:** Публичный IP (или домен)

```kotlin
// В Constants.kt
const val BASE_URL = "https://51.250.123.45/api/v1/"
// Или лучше использовать домен:
const val BASE_URL = "https://api.trusttheroute.com/api/v1/"
```

---

## 🔒 Безопасность

### Публичный IP:

**Риски:**
- Доступен из интернета
- Может быть атакован
- Нужна защита (firewall, SSH ключи)

**Защита:**
- ✅ Используйте SSH ключи вместо паролей
- ✅ Настройте firewall (только нужные порты)
- ✅ Включите защиту от DDoS (в Yandex Cloud)
- ✅ Используйте fail2ban для защиты от брутфорса

### Внутренний IP:

**Преимущества:**
- Недоступен из интернета
- Безопаснее по умолчанию
- Бесплатный трафик

**Использование:**
- Для подключения между сервисами в облаке
- Для подключения к базе данных

---

## 📝 Где найти IP адреса

### В консоли Yandex Cloud:

1. Откройте раздел **"Compute Cloud" → "Виртуальные машины"**
2. Выберите вашу ВМ
3. В разделе **"Сеть"** вы увидите:
   - **Внутренний IPv4:** `10.128.0.5`
   - **Публичный IPv4:** `51.250.123.45`

---

## 🌍 Рекомендации по архитектуре

### Правильная архитектура:

```
┌─────────────────────────────────────────┐
│         Интернет                         │
│                                          │
│  ┌──────────────────┐                  │
│  │  Ваш компьютер    │                  │
│  │  (SSH, API)       │                  │
│  └─────────┬─────────┘                  │
│            │                             │
│            │ Публичный IP                │
│            ▼                             │
│  ┌──────────────────┐                   │
│  │  ВМ (Backend)    │                   │
│  │  Публичный:      │                   │
│  │  51.250.123.45   │                   │
│  │  Внутренний:     │                   │
│  │  10.128.0.5      │                   │
│  └─────────┬─────────┘                   │
│            │                             │
│            │ Внутренний IP               │
│            ▼                             │
│  ┌──────────────────┐                   │
│  │  PostgreSQL      │                   │
│  │  Внутренний:     │                   │
│  │  10.128.0.10     │                   │
│  └──────────────────┘                   │
└─────────────────────────────────────────┘
```

---

## ✅ Итоговая рекомендация

### Для SSH подключения с вашего компьютера:

**✅ Используйте: ПУБЛИЧНЫЙ IP адрес**

```bash
ssh -i ~/.ssh/yandex_vm_key ubuntu@<ПУБЛИЧНЫЙ-IP>
```

### Для подключения бэкенда к базе данных:

**✅ Используйте: ВНУТРЕННИЙ IP базы данных**

---

## 🔧 Настройка домена (рекомендуется)

Вместо использования IP адреса напрямую, лучше настроить домен:

1. **Купите домен** (например, `trusttheroute.com`)
2. **Настройте DNS запись:**
   ```
   A запись: api.trusttheroute.com → 51.250.123.45
   ```
3. **Используйте домен в приложении:**
   ```kotlin
   const val BASE_URL = "https://api.trusttheroute.com/api/v1/"
   ```

**Преимущества:**
- Не нужно менять IP в коде при смене сервера
- Профессиональнее
- Можно использовать SSL сертификат (Let's Encrypt бесплатно)

---

## 💡 Советы

1. **Сохраните оба IP адреса** — они понадобятся для разных задач
2. **Публичный IP может измениться** — если используете динамический IP
3. **Для production используйте домен** — вместо прямого IP
4. **Внутренний IP стабильнее** — для подключения между сервисами

---

**Последнее обновление:** 2024
