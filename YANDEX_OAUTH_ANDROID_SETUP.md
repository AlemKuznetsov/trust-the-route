# Настройка Android-приложения в Yandex OAuth

## 📋 Что нужно указать в форме Yandex OAuth

### 1. Android package name (обязательно)

**Что это:** Уникальный идентификатор вашего Android приложения

**Где найти:**
- Откройте файл `app/build.gradle.kts`
- Найдите строку `applicationId`
- В нашем случае это: `com.trusttheroute.app`

**Что указать:**
```
com.trusttheroute.app
```

---

### 2. SHA256 Fingerprints (обязательно)

**Что это:** Отпечаток сертификата, которым подписано приложение. Yandex использует его для проверки подлинности приложения.

**Как получить:**

#### Вариант A: Через Android Studio (для debug-сертификата)

1. Откройте Android Studio
2. Откройте терминал (Terminal) внизу экрана
3. Выполните команду:
   ```bash
   keytool -list -v -keystore ~/.android/debug.keystore -alias androiddebugkey -storepass android -keypass android
   ```
4. Найдите строку `SHA256:` и скопируйте значение (без пробелов)

#### Вариант B: Через командную строку (Windows)

1. Откройте PowerShell или CMD
2. Перейдите в папку с JDK (обычно `C:\Program Files\Java\jdk-XX\bin`)
3. Выполните команду:
   ```powershell
   keytool -list -v -keystore "%USERPROFILE%\.android\debug.keystore" -alias androiddebugkey -storepass android -keypass android
   ```
4. Найдите строку `SHA256:` и скопируйте значение

#### Вариант C: Через Gradle (автоматически)

Добавьте в `app/build.gradle.kts`:

```kotlin
android {
    // ... существующий код ...
    
    signingConfigs {
        getByName("debug") {
            // Gradle автоматически покажет SHA256 при сборке
        }
    }
}
```

Затем выполните:
```bash
./gradlew signingReport
```

В выводе найдите `SHA256:` для debug-конфигурации.

---

### 3. Android Google Play URL (опционально)

**Что это:** Ссылка на ваше приложение в Google Play Store

**Что указать:**
- Если приложение уже опубликовано в Google Play:
  ```
  https://play.google.com/store/apps/details?id=com.trusttheroute.app
  ```
- Если приложение еще не опубликовано:
  - Можно оставить пустым
  - Или указать позже, когда опубликуете

---

## 📝 Пример заполнения формы

```
Платформы приложений: Android-приложение ✓

Android package name: com.trusttheroute.app

Android Google Play URL: (можно оставить пустым)

SHA256 Fingerprints: AA:BB:CC:DD:EE:FF:00:11:22:33:44:55:66:77:88:99:AA:BB:CC:DD:EE:FF:00:11:22:33:44:55:66:77:88:99
```

**⚠️ ВАЖНО:** 
- SHA256 Fingerprints должен быть в формате с двоеточиями: `AA:BB:CC:...`
- Для продакшена нужно будет добавить SHA256 от release-сертификата
- Можно добавить несколько SHA256 (для debug и release)

---

## 🔍 Как проверить, что все правильно

После заполнения формы:

1. Сохраните настройки
2. Вернитесь к настройкам приложения
3. Убедитесь, что Android-платформа отображается как активная
4. Скопируйте Client ID
5. Добавьте Client ID в `YandexAuthManager.kt`

---

## 🚀 Следующие шаги

После настройки Android-платформы:

1. ✅ Скопируйте Client ID
2. ✅ Добавьте Client ID в код (см. `YANDEX_ID_OAUTH_SETUP.md`)
3. ✅ Протестируйте авторизацию в приложении

---

## ❓ Часто задаваемые вопросы

### Q: Нужен ли SHA256 для debug и release отдельно?

A: Да, рекомендуется добавить оба:
- Debug SHA256 - для разработки и тестирования
- Release SHA256 - для продакшена

### Q: Как получить SHA256 для release-сертификата?

A: Если у вас уже есть release keystore:
```bash
keytool -list -v -keystore путь/к/вашему/keystore.jks -alias ваш_alias
```

### Q: Что делать, если SHA256 не работает?

A: Проверьте:
1. Правильность формата (с двоеточиями)
2. Что используете правильный keystore (debug или release)
3. Что приложение подписано тем же сертификатом

---

**Статус:** ✅ Готово к использованию  
**Дата:** 2024
