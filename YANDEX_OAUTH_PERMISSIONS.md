# Настройка прав доступа в Yandex OAuth

## 📋 Какие права доступа нужны нашему приложению

Для работы авторизации через Yandex ID нам нужны следующие данные пользователя:

### ✅ Обязательные права (отметьте эти чекбоксы):

1. **"Доступ к адресу электронной почты"** ✅
   - Уже отмечено на скриншоте
   - Нужно для идентификации пользователя и связи с аккаунтом

2. **"Доступ к логину, имени и фамилии, полу"** ✅
   - Нужно отметить
   - Используется для отображения имени пользователя в приложении
   - В коде: `login:name` scope

3. **"Доступ к портрету пользователя"** ✅
   - Нужно отметить
   - Используется для отображения аватара пользователя (опционально)
   - В коде: `login:avatar` scope

### ❌ Не нужны (не отмечайте):

- ❌ "Доступ к дате рождения" - не используется
- ❌ "Доступ к номеру телефона" - не используется

---

## 📝 Итоговая настройка

**Отметьте в форме:**

```
✅ Доступ к адресу электронной почты
✅ Доступ к логину, имени и фамилии, полу
✅ Доступ к портрету пользователя
```

**Не отмечайте:**
```
❌ Доступ к дате рождения
❌ Доступ к номеру телефона
```

---

## 🔍 Соответствие с кодом

В файле `YandexAuthManager.kt` указаны scopes:
```kotlin
private const val SCOPES = "login:email login:avatar login:name"
```

Эти scopes соответствуют правам доступа:
- `login:email` → "Доступ к адресу электронной почты"
- `login:avatar` → "Доступ к портрету пользователя"
- `login:name` → "Доступ к логину, имени и фамилии, полу"

---

## ⚠️ Важно

- Запрашивайте только необходимые права (как указано в инструкции на форме)
- Не запрашивайте лишние данные (дата рождения, телефон), если они не нужны
- Это повышает доверие пользователей и соответствует принципам privacy

---

**Статус:** ✅ Готово к настройке  
**Дата:** 2024
