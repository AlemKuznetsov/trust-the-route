# Конфигурация виртуальной машины для бэкенда

Рекомендации по настройке виртуальной машины в Yandex Cloud для бэкенд API приложения.

---

## 📋 Пошаговая настройка

### 1. Образ загрузочного диска

**✅ Выберите:** **"Marketplace"** → Найдите **"Ubuntu"** → Выберите **"Ubuntu 22.04 LTS"** или **"Ubuntu 24.04 LTS"**

**Почему:**
- Стабильная и популярная ОС для серверов
- Хорошая документация и поддержка
- Подходит для Node.js, Python, Kotlin/Ktor

**Альтернативы:**
- Ubuntu 20.04 LTS (если нужна более старая версия)
- Debian (легче, но менее популярна)

---

### 2. Расположение

**✅ Оставьте:** **"ru-central1-d"** (или выберите ближайшую зону)

**Почему:**
- Близко к России (низкая задержка)
- Можно выбрать любую зону ru-central1 (a, b, c, d)
- Для начала не критично

---

### 3. Диски и файловые хранилища

**✅ Настройки:**

- **Тип диска:** **SSD** (рекомендуется) или **HDD** (дешевле, но медленнее)
- **Размер:** **20 GB** (достаточно для начала)
  - Можно увеличить позже при необходимости
  - Для ОС + бэкенд приложения 20 GB хватит

**Рекомендация:**
- Начните с **20 GB SSD**
- Если бюджет ограничен → можно **20 GB HDD** (экономия ~100-150₽/месяц)

---

### 4. Вычислительные ресурсы

**✅ Выберите:** **"Standard"** → **"Intel | 2 vCPU | 4 ГБ RAM"**

**Почему:**
- **2 vCPU** — достаточно для бэкенд API с небольшой нагрузкой
- **4 GB RAM** — комфортно для Node.js/Python/Kotlin приложений
- Можно масштабировать позже при росте нагрузки

**Альтернативы:**
- **2 vCPU | 2 GB RAM** — минимум (экономия ~200₽/месяц, но может быть тесно)
- **4 vCPU | 4 GB RAM** — если планируется высокая нагрузка сразу

**НЕ выбирайте:**
- ❌ High memory — избыточно для бэкенда
- ❌ HighFreq — для CPU-интенсивных задач
- ❌ GPU — для машинного обучения, не для бэкенда

---

### 5. Сетевые настройки

**✅ Настройки:**

- **Подсеть:** Оставьте по умолчанию (создастся автоматически)
- **Публичный IP-адрес:** **"Автоматическое"** (обязательно!)
  - Это нужно для доступа к серверу из интернета
  - Без публичного IP бэкенд будет недоступен
- **Защита от DDoS-атак:** **"Включить"** (рекомендуется)
  - Базовая защита включена бесплатно
- **Группы безопасности:** Оставьте по умолчанию
  - Позже можно настроить правила firewall

**Важно:**
- Публичный IP обязателен для работы API
- После создания ВМ получите IP адрес для подключения

---

### 6. Доступ

**✅ Настройки:**

**Вариант 1: SSH-ключ (рекомендуется)**
- **SSH-ключ:** **"Добавить ключ"**
- Создайте SSH ключ на вашем компьютере:
  ```bash
  ssh-keygen -t rsa -b 4096 -C "your_email@example.com"
  ```
- Скопируйте публичный ключ (файл `~/.ssh/id_rsa.pub`) и добавьте в форму
- Это безопаснее, чем пароль

**Вариант 2: Логин и пароль**
- **Имя администратора ВМ:** `ubuntu` (для Ubuntu)
- **Пароль:** Создайте надежный пароль
- Менее безопасно, но проще для начала

**Рекомендация:** Используйте SSH ключ для безопасности

---

### 7. Резервное копирование

**⚠️ Опционально:**

- **Включить резервное копирование:** Можно включить позже
- Для начала можно отключить (экономия)
- Рекомендуется включить для production

**Стоимость:** ~10-15% от стоимости ВМ

---

### 8. Общая информация

**✅ Настройки:**

- **Имя ВМ:** `trust-the-route-backend` (или любое понятное имя)
- **Описание:** "Backend API для Trust The Route"
- **Метки:** Можно добавить для организации (например: `env=production`, `app=backend`)

---

## 💰 Анализ стоимости

### Текущая конфигурация (из скриншота):

```
Вычислительные ресурсы (Intel Ice Lake, 100% vCPU): 1 660,18 ₽
RAM: 442,71 ₽
Публичный IP: 109,73 ₽
SSD диск: 261,54 ₽
─────────────────────────────────────
ИТОГО: ~2 554,17 ₽/месяц
```

### Оптимизированная конфигурация (рекомендуется):

**Вариант 1: Минимальный (для тестирования)**
- 2 vCPU | 2 GB RAM | 20 GB SSD
- **Стоимость:** ~1 500-1 800₽/месяц

**Вариант 2: Рекомендуемый (для начала)**
- 2 vCPU | 4 GB RAM | 20 GB SSD
- **Стоимость:** ~2 000-2 500₽/месяц

**Вариант 3: Текущий (из скриншота)**
- 2 vCPU | 4 GB RAM | 20 GB SSD
- **Стоимость:** ~2 554₽/месяц

---

## ✅ Итоговая рекомендация

### Выберите следующие параметры:

```
✅ Образ: Ubuntu 22.04 LTS (из Marketplace)
✅ Расположение: ru-central1-d (любая зона)
✅ Диск: 20 GB SSD
✅ Вычислительные ресурсы: Standard → Intel | 2 vCPU | 4 ГБ RAM
✅ Публичный IP: Автоматическое (ОБЯЗАТЕЛЬНО!)
✅ Защита от DDoS: Включить
✅ Доступ: SSH-ключ (рекомендуется) или Логин/Пароль
⚠️ Резервное копирование: Можно отключить для начала
✅ Имя: trust-the-route-backend
```

### Ожидаемая стоимость:

**~2 000-2 500₽/месяц** (зависит от текущих тарифов)

---

## 🚀 После создания ВМ

### Шаги для настройки бэкенда:

1. **Подключитесь по SSH:**
   ```bash
   ssh ubuntu@<ваш-ip-адрес>
   ```

2. **Обновите систему:**
   ```bash
   sudo apt update && sudo apt upgrade -y
   ```

3. **Установите необходимое ПО:**
   - Node.js (если используете Node.js)
   - Или Python (если используете Python)
   - Или JDK (если используете Kotlin/Ktor)

4. **Настройте firewall:**
   ```bash
   sudo ufw allow 22    # SSH
   sudo ufw allow 80    # HTTP
   sudo ufw allow 443   # HTTPS
   sudo ufw enable
   ```

5. **Разверните бэкенд API**

---

## 💡 Советы по оптимизации

### Если бюджет ограничен:

1. **Начните с меньших ресурсов:**
   - 2 vCPU | 2 GB RAM вместо 4 GB
   - Экономия ~200-300₽/месяц

2. **Используйте HDD вместо SSD:**
   - Экономия ~100-150₽/месяц
   - Медленнее, но для начала приемлемо

3. **Отключите резервное копирование:**
   - Экономия ~200-300₽/месяц
   - Делайте бэкапы вручную

4. **Используйте динамический IP:**
   - Статический IP стоит дороже
   - Для начала динамический подойдет

### Минимальная конфигурация:

- 2 vCPU | 2 GB RAM | 20 GB HDD
- **Стоимость:** ~1 200-1 500₽/месяц

---

## ⚠️ Важные замечания

1. **Публичный IP обязателен** — без него бэкенд будет недоступен
2. **SSH ключ безопаснее пароля** — рекомендуется использовать
3. **Защита от DDoS** — включите для базовой защиты
4. **Можно масштабировать позже** — начните с минимальной конфигурации

---

## 📝 Чеклист перед созданием

- [ ] Выбран образ Ubuntu 22.04 LTS
- [ ] Выбрана конфигурация 2 vCPU | 4 GB RAM
- [ ] Настроен диск 20 GB SSD
- [ ] Включен публичный IP адрес
- [ ] Добавлен SSH ключ или настроен пароль
- [ ] Включена защита от DDoS
- [ ] Указано понятное имя ВМ

---

**Готово к созданию!** После создания ВМ вы получите IP адрес для подключения и настройки бэкенда.

---

**Последнее обновление:** 2024
