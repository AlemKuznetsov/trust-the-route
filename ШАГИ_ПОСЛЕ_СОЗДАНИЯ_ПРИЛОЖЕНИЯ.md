# Что делать после создания приложения в Yandex OAuth

## ✅ Шаг 1: Найдите Client ID

1. В настройках вашего приложения в Yandex OAuth найдите поле **"ID приложения"** или **"Client ID"**
2. Скопируйте этот ID (это строка вида `abc123def456...`)

## 🔑 Шаг 2: Добавьте Client ID в код

### Вариант A: Простой способ (для разработки)

1. Откройте файл: `app/src/main/java/com/trusttheroute/app/data/auth/YandexAuthManager.kt`
2. Найдите строку 38:
   ```kotlin
   private const val YANDEX_CLIENT_ID = "YOUR_YANDEX_CLIENT_ID_HERE"
   ```
3. Замените на:
   ```kotlin
   private const val YANDEX_CLIENT_ID = "ваш_client_id_здесь"
   ```
   Например:
   ```kotlin
   private const val YANDEX_CLIENT_ID = "abc123def456ghi789jkl012mno345pqr678stu901vwx234yz"
   ```

### Вариант B: Безопасный способ (рекомендуется для продакшена)

1. Откройте файл `local.properties` в корне проекта (создайте, если его нет)
2. Добавьте строку:
   ```properties
   YANDEX_CLIENT_ID=ваш_client_id_здесь
   ```
3. Откройте `app/build.gradle.kts`
4. Добавьте в секцию `android { defaultConfig {`:
   ```kotlin
   buildConfigField("String", "YANDEX_CLIENT_ID", "\"${project.findProperty("YANDEX_CLIENT_ID") ?: ""}\"")
   ```
5. Обновите `YandexAuthManager.kt`:
   ```kotlin
   private val YANDEX_CLIENT_ID = BuildConfig.YANDEX_CLIENT_ID
   ```

## ✅ Шаг 3: Проверьте настройки

Убедитесь, что в настройках приложения Yandex OAuth:

- ✅ **Redirect URI** указан: `trusttheroute://oauth/yandex`
- ✅ **Права доступа** выбраны:
  - Доступ к адресу электронной почты
  - Доступ к логину, имени и фамилии, полу
  - Доступ к портрету пользователя
- ✅ **Android package name**: `com.trusttheroute.app`
- ✅ **SHA256 Fingerprints** указан

## 🧪 Шаг 4: Протестируйте

1. Соберите и запустите приложение
2. На экране входа нажмите **"Войти через Yandex ID"**
3. Должен открыться браузер с формой авторизации Yandex
4. После успешной авторизации вы вернетесь в приложение
5. Проверьте, что пользователь авторизован

## ❓ Если что-то не работает

### Проблема: "Неверный Client ID"
- Проверьте, что Client ID скопирован полностью, без пробелов
- Убедитесь, что Client ID в кавычках в коде

### Проблема: "Redirect URI не совпадает"
- Проверьте, что в настройках Yandex OAuth указан точно: `trusttheroute://oauth/yandex`
- Убедитесь, что в коде `REDIRECT_URI` совпадает

### Проблема: "Недостаточно прав доступа"
- Проверьте, что в настройках приложения выбраны все нужные права доступа
- Убедитесь, что scopes в коде соответствуют выбранным правам

## 📝 Чеклист

- [ ] Client ID скопирован из настроек приложения
- [ ] Client ID добавлен в `YandexAuthManager.kt`
- [ ] Redirect URI проверен: `trusttheroute://oauth/yandex`
- [ ] Права доступа выбраны в настройках
- [ ] Android package name указан: `com.trusttheroute.app`
- [ ] SHA256 Fingerprints указан
- [ ] Приложение собрано и запущено
- [ ] Авторизация протестирована

---

**Готово!** После выполнения этих шагов авторизация через Yandex ID должна работать.
